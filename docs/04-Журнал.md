# Журнал

Что сделано и в каком оно состоянии. Отвечает на вопрос «где мы сейчас».
Обновляется в конце каждой беседы. Новые записи сверху.

---

## 25.09.2026 — передача: «Тотальный Учёт» прошёл Этапы 0–2; следующая сессия ядра — по его промпту

Кода и решений нет — сверка перед `/clear`. Ядро — `ee1dc37`, дерево чистое.

**Где семья** (`git show` и `ls-remote` клонов, 25.09.2026):

- **«Тотальный Учёт»** — `fc08977`: Этапы 0, 1, 2 закрыты, проверены на телефоне. «Сводка» показала настоящий `summary.json` «Делу Время» — числа совпали с приложением (его Журнал, 25.09.2026, «Этап 1…»). Этим выполнено условие закрытия Этапа 3 ядра — **закрывает следующая сессия ядра**, не эта. Его Этап 3 («рядом», Я-19, п. 5) ждёт среза «Дневников» (Р-15 «Тотального Учёта»)
- **Срез отдают** «Делу Время» и «Трапеза» (Р-55 «Трапезы», `9911c2d`); у «Дневников» и «Злотых» функции среза нет. Строка «Трапезы» в «Составе» `02` устарела — сверка в следующей сессии
- Решения «Тотального Учёта» после Р-08, которые касаются ядра: Р-09 (заглушка `noSync` вместо `sync` в `CoreProvider`), Р-12 (`canWrite` у fine-grained токена — права аккаунта, не токена), Р-15
- **Токен «семья»** — перешёл ли человек, в документах не записано

**Следующая сессия ядра** — промпт в Журнале `TotalUchet`, запись 25.09.2026 «Этап 3 не начат: ждёт среза „Дневников“…», раздел «Сессия ядра — из корня `FamilyCore`» (`fc08977`). Шесть пунктов: закрыть Этап 3; строка «Трапезы» в «Составе»; сверить промпты срезов «Дневникам» и «Злотым», ключи тренировок — устойчивые; `canWrite` (`core/github.ts`) — комментарий и где ядро на него опирается; `sync` в `CoreProvider` необязательным (правка шва — Я-NN); «подключение в пару кликов» — только на обсуждение.

---

## 24.09.2026 — Я-27: токен «семья» — один на все устройства

**Решение** — **Я-27**, выбор человека против рекомендации: один токен «семья» на все устройства вместо «свой на каждом» (отменяет эту часть Я-25); токен чтения метаприложения — тоже один (та же часть Я-16). Перенос между устройствами — менеджером паролей, не мессенджером. Цена: потеря устройства — отзыв единственного токена, встают все приложения везде до перевписывания.

**Сделано** — `05` Я-27; `02` «Устройство и origin» — токены; `06` «Токены семьи» — шаги под один токен: «Заменить» в каждом приложении, репозиторий и ветку не трогать, что делать при потере устройства; План — тексты приложений, где записано «свой токен на каждом устройстве» (перечень там), — их сессиями при следующем заходе.

**Промпт «Тотального Учёта»** (запись «итоги первой сессии…», Этап 0) дополнен строкой о Я-27 — копировать оттуда.

**Следующий шаг** — человек: «семья» по `06-Процесс`; `/clear`; сессия из `TotalUchet`.

---

## 24.09.2026 — Я-24 в семье: четыре приложения на `6a5da3f`, `app` в `meta.json` у всех; токен «семья» — можно

Сверено через `git show HEAD` и `ls-remote` клонов: указатель `src/shared` — `6a5da3f` у «Делу Время» (`03b8f44`), «Трапезы» (`4b8a978`), «Дневников» (`26ac2f6`), «Злотых» (`371fe04`); клоны совпадают с `origin`. `app` в `meta.json` репозиториев данных проверен человеком у всех четырёх — записано в их Журналах; у «Дневников» — `{ "app": "dnevniki", "schemaVersion": 2 }`: давний файл принят, имя дописалось. Их прогоны: тесты, `tsc`, сборка, smoke — с кодом 0.

**Сделано здесь**

- `06-Процесс` — раздел «Токены семьи (Я-25)»: выпуск по шагам, отзыв прежних, новое приложение правкой токена, токен чтения метаприложения
- План — раздел «Я-24 и токен „семья“»: остались токен человека, справки двух приложений, указатель метаприложения

**Принесено сессиями приложений**

- **Поправка к записи ниже:** `origin` «Дневников» был не впереди клона, а позади — `baa8102` лежал локально незапушенным. Ушёл с их сессией
- **`npm run typecheck` есть только у «Злотых».** У «Делу Время», «Трапезы», «Дневников» типы проверяет `tsc` внутри `build`. В следующих промптах приложениям — «`tsc --noEmit` или `typecheck`, что есть»
- **Справка «Синхронизация» (`Help.tsx`) «Делу Время» и «Трапезы»** говорит «токен к своему репозиторию» — при переходе на «семью» поправить их сессиями (их Планы)
- «Дневники», `07-Установка`: основной путь остаётся токеном на свой репозиторий, «семья» — дополнением; README данных руками класть больше не нужно — кладёт синхронизация. Их документы, на их обсуждение
- `SyncSettings` ядра пошаговой инструкции выпуска токена не содержит — править нечего; «Проверить доступ» показывает полное имя репозитория, на этом держится шаг 5 выпуска

**Следующий шаг**

1. Человек — токен «семья» по `06-Процесс`, «Токены семьи»; прежние отозвать
2. `/clear`; сессия из `TotalUchet` по промпту записи ниже («Промпт „Тотальному Учёту“ — Этап 0») — он в силе без правок, указатель ядра — не старше `6a5da3f`
3. Сюда — короткими сессиями: ключи срезов «Трапезы», «Дневников», «Злотых» в «Состав»; закрытие Этапа 3 по Этапу 1 метаприложения; `dbName` «Дачи»

---

## 24.09.2026 — итоги первой сессии «Тотального Учёта»: Я-23…Я-26; `meta.json` называет приложение

Вход — список «Для сессии ядра» Журнала `TotalUchet` (`f598cae`), прочитан через `git show` вместе с его `CLAUDE.md`, `02`, `05` (Р-01…Р-08).

**Решения** — выбор человека, рекомендации:

- **Я-23** — «Тотальный Учёт» на ядре без синхронизации, `dbName` `totaluchet` в договоре; поля импорта `AppConfig` остаются обязательными, у приложения без импорта — заглушки
- **Я-24** — `meta.json` — `{ app, schemaVersion }`, `app` — `dbName`; чужой — проход стоит, ничего не скачав и не отправив; давний без имени принимается, имя дописывается. Правка договора, релиз на все
- **Я-25** — токен «семья» с записью у приложений, только на репозитории данных, свой на каждом устройстве; у метаприложения — отдельный на чтение. Переход — после Я-24
- **Я-26** — `share` — доля, 1 — целое; не ограничена 0…1; `format` прежний

**Сделано**

- `05` — Я-23…Я-26; открытые вопросы: экран динамики по архиву (Р-08 «Тотального Учёта») — пересмотр Я-15 до его кода; токен один раз на устройство для всех приложений
- `02` — «Шов»: приложение без импорта; договор: `meta.json` с `app`, `totaluchet`, токены, `share`
- `01` — «Тотальный Учёт» в «Кто берёт ядро»; `06` — он в «Где что живёт», только читать
- План — Этап 3 закрывается Этапом 1 метаприложения; раздел «Я-24 и токен „семья“»; своё название приложения параметром ядра — в «Отложено»
- Код (`449da39`): `metaFile(app, schemaVersion)`, `parseMeta` сверяет `app`, проход проверяет `meta.json` по своему отпечатку, README данных — «чьи данные и версия схемы», комментарий `share` в `core/summary.ts`

**Находка по ходу — дыра шире, чем в предложении.** `meta.json` проверялся, только если разошёлся с запомненным отпечатком. Запомненное дерево при смене имени репозитория не сбрасывается, а давний `{"schemaVersion": 1}` у двух приложений побайтно одинаков. Приложение, вписавшее имя репозитория соседа с той же версией схемы, **не проверяло его вовсе** даже со своими токенами — просто сейчас его останавливает 404. Теперь проверка — по своему отпечатку. Тест, падающий без правки прохода, — «чужой meta.json проверяется, даже если его отпечаток запомнен».

**Ответы без решений**

- **Тексты 403/404 клиента GitHub** — не чиним: `GitHubError` несёт `status`, читатель переводит по нему своими словами (Р-05 «Тотального Учёта»). Для читателя неверен только текст 403 про «Read and write»
- **Своё название приложения** — не параметр ядра: просит одно приложение; переименование вкладок «Делу Время» — другое. План, «Отложено»

**Проверено машинно:** `npm test` — 21 файл, 386 тестов; `npm run typecheck` — оба с кодом 0. Без правки прохода два новых теста падают.

**Не проверено:** настоящий проход с давним `meta.json` — в приложениях при обновлении указателя; ping-pong `meta.json` со старой сборкой на втором устройстве (цена Я-24, п. 2) — ожидаем, не видели.

**Что обновить в корне каждого приложения**

- **«Делу Время», «Трапеза», «Дневники», «Злотые»** — сессией из своего корня: `git submodule update --remote src/shared`; тесты, `typecheck`, сборка, `smoke` по коду выхода; коммит указателя, пуш. Потом **синхронизировать хотя бы раз** с любого устройства — в `meta.json` репозитория данных ляжет `app`. Тесты приложений сверяют путь `meta.json`, а не содержимое, — ломаться нечему. Видимого человеку нет, строка в `changes.ts` не нужна. Если своя `02-Архитектура` пересказывает `meta.json { schemaVersion }` — поправить ссылкой на договор ядра. Устройства обновляются сами: работник — `autoUpdate` со `skipWaiting` (`sw.ts`), новая сборка забирает страницу при первом открытии с сетью. Ping-pong `meta.json` (цена Я-24, п. 2) — не больше одного лишнего коммита на устройство, которое открыли впервые после деплоя: автосинхронизация успевает пройти старой сборкой до подмены. Делать руками ничего не нужно
- **Токен «семья»** — человек, после того как все четыре репозитория данных получили `app` (План, раздел «Я-24…»): выпустить на каждом устройстве — Contents: Read and write, только репозитории данных, — вписать во все приложения, имя репозитория сверить «Проверить доступ»
- **«Тотальный Учёт»** — Этап 0 берёт указатель не старше `449da39`. В его `02`: `share` — доля, 1 — целое (Я-26), вместо «не решено»; поля импорта — заглушки подтверждены (Я-23); `dbName` в договоре. В `05` снять открытые вопросы «Единица `share`» (Я-26) и оставить «Экран динамики» — он теперь и в открытых вопросах ядра
- **«Дача»** — с первой строки на ядре с Я-24; её `dbName` — в договор сессией ядра при старте

**Указатели ядра у приложений на 24.09.2026** (`git show HEAD`, `ls-remote`): «Делу Время» — `b0047b6`; «Трапеза» и «Злотые» — `632c7c6`, до кода среза; «Дневники» — `897f3ae`, и **их `origin` впереди локального клона** (`b8ea708` против `baa8102`) — сессия начинает с `git pull`. Скачок указателя приносит код среза (`d049c2c`, Я-20, Я-21): у приложения без функции `summary` он ничего не меняет.

### Промпты сессиям приложений — Я-24, обновление указателя

Одна сессия на приложение, из его корня, в любом порядке. Срез итогов «Трапезы», «Дневников», «Злотых» (промпты записи «Этап 3, пп. 2–3») — отдельно; можно той же сессией после указателя.

**«Делу Время»** — `C:\Ф\Prognananda\Claude Projs\ТаймМенеджмент\DeluVremya`:

```
Сопровождение: обновление ядра FamilyCore до Я-24 (meta.json называет
приложение). Прочитай CLAUDE.md, docs/04-Журнал.md и 03-План.md, затем
в src/shared/docs после обновления: 05-Решения.md — Я-23…Я-26,
04-Журнал.md — запись «итоги первой сессии „Тотального Учёта“».

1. git submodule update --remote src/shared — указатель с b0047b6 на HEAD
   ядра (не старше 4b24129). В src/shared ничего не править и npm install
   не запускать. Что приехало: Я-21 (комментарии среза), Я-24 — meta.json
   { app, schemaVersion }, app = dbName; чужой app — проход стоит; давний
   без app принимается, имя дописывается. Я-26 — share = доля.
2. npm test, npm run typecheck, npm run build, smoke — итог по коду выхода.
   Падает из-за ядра — стоп, сказать мне: это сессия ядра, не правка на месте.
3. Документы: docs/02-Архитектура.md, раскладка — «meta.json { schemaVersion }»
   (стр. ~148) и абзац «meta.json содержит schemaVersion» (стр. ~380) —
   под Я-24, ссылкой на договор ядра, не пересказом. Инструкция по токену,
   если она у вас есть, — под Я-25: один токен «семья» с Contents: Read and
   write только на репозитории данных семьи, свой на каждом устройстве;
   с оговоркой «переход — когда все четыре приложения синхронизировались
   на ядре с Я-24» (План ядра, «Я-24 и токен „семья“»).
4. changes.ts — строки не нужно: видимого нет.
5. Коммит указателя и документов, пуш, запись в Журнал.
6. Мне: открыть приложение с сетью и синхронизировать хотя бы раз —
   в meta.json репозитория данных должно появиться "app": "deluvremya".
   Проверить на GitHub глазами.
```

**«Трапеза»** — `C:\Ф\Prognananda\Claude Projs\Еда\Trapeza`: промпт «Делу Время» с заменами — указатель **с `632c7c6`**, приезжает ещё код среза (`d049c2c`, Я-16…Я-21): без функции `summary` в `AppConfig` ничего не меняется, `summary.json` не пишется; в `02` — раскладка (стр. ~131), «Договор семьи» (стр. ~338), `meta.json` (стр. ~348); ожидаемое в `meta.json` — `"app": "trapeza"`. Срез «Трапезы» — промпт записи «Этап 3, пп. 2–3», можно этой же сессией после п. 5.

**«Дневники»** — `C:\Ф\Prognananda\Claude Projs\Дневники\Dnevniki`: промпт «Делу Время» с заменами — **сначала `git pull`**: `origin` впереди клона; указатель **с `897f3ae`** (или с того, что в подтянутом), приезжает код среза, как у «Трапезы»; в `02` — раскладка (стр. ~150), `meta.json` (стр. ~346); **`docs/07-Установка.md`** — токен (стр. ~160: сейчас «Read and write» на один репозиторий) под Я-25 с оговоркой о переходе, и строка «`meta.json` — версия схемы данных» (стр. ~201) → «чьи данные и версия схемы»; ожидаемое — `"app": "dnevniki"`.

**«Злотые»** — `C:\Ф\Prognananda\Claude Projs\Злотые`: промпт «Делу Время» с заменами — указатель **с `632c7c6`**, приезжает код среза, как у «Трапезы»; в `02` — раскладка (стр. ~239–242), «Договор семьи» (стр. ~381), `schemaVersion` (стр. ~385); ожидаемое — `"app": "zloty"`. Срез «Злотых» — промпт записи «Этап 3, пп. 2–3».

**После всех четырёх** — человек: в каждом репозитории данных `meta.json` с `app`; потом токен «семья» (Я-25) на каждом устройстве, имя репозитория в каждом приложении — «Проверить доступ».

### Промпт «Тотальному Учёту» — Этап 0; заменяет промпт его `06-Процесс`, «Передача»

```
Проект «Тотальный Учёт» (TotalUchet) — метаприложение семьи: читает
summary.json приложений семьи и показывает неделю и месяц на одном экране.
Работать в корне C:\Ф\Prognananda\Claude Projs\TotalUchet.

Прочитай CLAUDE.md и все файлы docs/ перед началом. Договор семьи —
в ядре: C:\Ф\Prognananda\Claude Projs\FamilyCore\docs\02-Архитектура.md,
«Договор семьи», «Срез итогов»; решения Я-10…Я-26 там же, в 05; ответ ядра
на твой список «Для сессии ядра» — 04-Журнал ядра, запись «итоги первой
сессии „Тотального Учёта“». До подмодуля ядро читается оттуда, только чтение.

Кода нет. Решения Р-01…Р-08 записаны 24.09.2026. Ответ ядра:
— Я-23: dbName 'totaluchet' в договоре — вписан, Этап 0 можно начинать;
  createSync не зовём; поля импорта AppConfig остаются обязательными —
  заглушки (promptRules: [], пустые строки), createImporting не зовём;
— Я-24: meta.json называет приложение — нас не касается, мы его не читаем
  и не пишем;
— Я-25: токены семьи — у приложений общий «семья» с записью, у нас
  отдельный только на чтение, только на репозитории данных (как Р-02);
— Я-27: токен чтения — один на все твои устройства, не «свой на каждом»;
  в 06 и Р-02 — ссылкой на Я-27 (Р-02 не переписывать: новая запись
  или строка в Журнале, как решишь);
— Я-26: share — доля, 1 — целое (0.25 — четверть), не ограничена 0…1;
  «25 %» — форматирование одного значения, не досчёт;
— тексты 403/404 ядра не меняются: свои по GitHubError.status (Р-05);
— своё название настройкой — параметром ядра не будет пока, Р-03 делаем у себя;
— экран динамики по архиву — открытый вопрос ядра, пересмотр Я-15 до его кода.

Первым делом — документы под ответ ядра, отдельным коммитом:
02-Архитектура — строка share в «Значения словами» («не решено» → доля,
Я-26), «Конфиг» — заглушки подтверждены (Я-23), dbName вписан;
05 — открытый вопрос «Единица share» снять ссылкой на Я-26 (новая запись
не нужна: старые не переписываются, открытые вопросы — список);
06 — «Передача» заменить этим промптом; Журнал — строкой.

Потом Этап 0 — «Каркас», по docs/03-План.md: план работ, код после
подтверждения. git submodule add https://github.com/goigolden88/FamilyCore.git
src/shared — берёт HEAD ядра, не старше 4b24129.

Ключевое до кода:
— ядро — подмодулем в src/shared (Р-02), отсюда не правится; нехватка —
  слово мне, это сессия ядра;
— своей синхронизации нет; свои записи — с Base, places заморожены после
  первого релиза (Я-09), брать их дословно из Архитектуры;
— id записи Seen — `${app}:${computedOn}`, не ULID (Р-08);
— репозиторий публичный (Р-01): ни токенов, ни настоящих срезов, ни названий
  групп — ни в коде, ни в тестах, ни в логах.

Как работаем: сначала план, код после подтверждения. Решения — в
docs/05-Решения.md в момент выбора. Отступления от Архитектуры и договора —
вслух до кода. В конце сессии — Журнал. Перед /clear дерево чистое и всё
запушено. Этап 3 ядра закрывается твоим Этапом 1 — «Сводка» на настоящем
summary.json «Делу Время»: когда будет, сказать мне для сессии ядра.
```

**Следующий шаг** — `/clear`; сессия из `TotalUchet` по промпту выше. Параллельно — четыре приложения по промптам выше, в любом порядке; после всех — токен «семья». Сюда — короткими сессиями: ключи срезов «Трапезы», «Дневников», «Злотых» в «Состав», закрытие Этапа 3 по Этапу 1 метаприложения, `dbName` «Дачи».

---

## 24.09.2026 — Я-22: «Дача» — в семью, тотализатор — вне; устаревшее в `01` и Плане; промпт метаприложения, окончательный

**Решение**

- **Я-22** — «Дача» — приложение семьи, сразу на ядре; тотализатор для друзей — вне семьи: данные общие на нескольких людей. Закрыт открытый вопрос «Берёт ли „Дача“ ядро». Выбор человека, рекомендация

**Сделано**

- `05` — Я-22; открытый вопрос о «Даче» снят
- `01-Ядро` **был устаревшим**: «Кто берёт ядро» говорил «заморожено», «переводится третьим», хотя семья на ядре целиком; «Не делаем: метаприложение» — до Я-10. Поправлено; добавлены «Дача», `TotalUchet`, тотализатор вне семьи
- `CLAUDE.md` — семья с «Дачей» и метаприложением; их репозитории отсюда только читаются
- План, «Дальше» — **был устаревшим**: «Дневники — перевод» после их перевода. Поправлено; «Дача» — сразу на ядре

**Стартовый промпт метаприложения — окончательный, заменяет промпт записи «репозиторий метаприложения заведён».** Сверх прежнего: список приложений не в коде — будет «Дача»; тотализатор не источник; репозиторий публичный; настоящий срез для проверки — у «Делу Время».

```
Первая сессия метаприложения семьи — репозиторий TotalUchet
(github.com/goigolden88/TotalUchet, публичный, пока пустой;
локально — C:\Ф\Prognananda\Claude Projs\TotalUchet).

Прочитай, только чтение, в C:\Ф\Prognananda\Claude Projs\FamilyCore:
CLAUDE.md; docs/01-Ядро.md; docs/02-Архитектура.md — «Договор семьи»,
особенно «Срез итогов» с таблицей «Состав»; docs/05-Решения.md — Я-10…Я-22
и открытые вопросы; docs/04-Журнал.md — записи 24.09.2026; docs/07-Разведка.md —
фон: что приложения умеют и чего не отдают; docs/06-Процесс.md — как ядро
раздаётся подмодулем. Образец настоящего среза — «Делу Время» (Р-87
«Делу Время», их docs/02-Архитектура.md, «Срез итогов»): читать через git show
в ТаймМенеджмент\DeluVremya. FamilyCore и репозитории приложений отсюда
не правятся: нужна правка договора — говоришь мне, это сессия ядра.

Уже решено ядром, здесь не переобсуждается:
- срез — summary.json в приватном репозитории данных каждого приложения;
  читается fine-grained токеном только на чтение; токен — в settings
  устройства, не синхронизируется; нет файла — «срез не отдаёт», не ошибка (Я-16);
- список приложений и имена их репозиториев — настройки человека, не код:
  сейчас отдаёт одно «Делу Время», дальше «Трапеза», «Дневники», «Злотые»,
  потом «Дача» (Я-22); у друга свои репозитории. Тотализатор — вне семьи,
  его не читаем;
- форма и проверка — core/summary.ts ядра, parseSummary (Я-17, Я-20);
- показываем, а не досчитываем: ни серий, ни средних на день, ни цвета
  по оценке, ни сложения строк разных приложений (Я-15, предварительно);
- through: null — отрезок закончился, но не заморожен; свежесть — computedOn
  и lastEdit; нет пункта «требует внимания» — хозяин не зовёт, а не
  «всё сделано» (Я-21);
- сопоставление строк соседей — по группе, настройкой человека (Я-19);
- репозиторий публичный: ни токенов, ни данных, ни примеров из настоящих
  срезов в нём не бывает — тесты на выдуманных срезах.

Сначала — документы, не код, по образцу семьи (skills doc-driven-project,
local-first-pwa): CLAUDE.md, docs/01…06. Развилки — с рекомендацией, спорное —
мне отдельными вопросами, решения — в момент выбора:
1. берёт ли TotalUchet ядро подмодулем (рекомендация ядра — да: parseSummary
   и типы, каркас стилей, Layout, settings, синхронизация не нужна или нужна —
   реши); свой dbName — его вписывает в договор сессия ядра;
2. первый экран — недельная сводка: что на нём, порядок приложений и строк,
   как видны свежесть (computedOn, lastEdit), неокончательность (through)
   и «не известно» с причиной;
3. «требует внимания» — сначала экраном; уведомление вместо четырёх —
   позже, вместе с выключением тем у приложений (Я-18, открытый вопрос);
4. экран «Семья»: ссылки на приложения и отметка «установлено», где браузер
   даёт узнать (getInstalledRelatedApps); установить соседа одной кнопкой
   браузер не позволяет — что работает, проверить на телефоне;
5. история увиденных срезов — нужна ли и где (Я-17: в срезе её нет);
6. Телеграм-бот — отдельным этапом после первого экрана: плановый workflow
   GitHub Actions читает срезы токеном из секретов и шлёт сводку через
   Bot API в выбранное время. Бот — новый названный потребитель (Я-13, Я-14),
   Телеграм — третья сторона: что ему уходит — правка состава сессией ядра.
   Бот с командами — позже, ему нужен сервер или вебхук. Сейчас — только
   записать в План.
План, решения, код — после моего подтверждения. Проверка на настоящем —
summary.json «Делу Время» из их репозитория данных, токен даю я.
Этап 3 ядра закрывается, когда первый экран читает настоящий summary.json.
```

**Следующий шаг** — `/clear` здесь; первая сессия из `Claude Projs\TotalUchet` с промптом выше. Параллельно — «Трапеза», «Дневники», «Злотые» по промптам записи «Этап 3, пп. 2–3». Сюда — короткими сессиями: ключи каждого среза в «Состав», `dbName` метаприложения и «Дачи» в договор.

---

## 24.09.2026 — репозиторий метаприложения заведён: `TotalUchet`; стартовый промпт

Человек завёл `github.com/goigolden88/TotalUchet` (публичный, пустой); склонирован отсюда в `Claude Projs\TotalUchet` — только клон, файлов не положено: документы пишет его первая сессия из его корня.

**Вопросы человека — ответы сессии ядра, не решения** (решения — в своих репозиториях; «Дача» — открытый вопрос `05`):

- **Установка всех приложений семьи из метаприложения** — одной кнопкой нельзя: у каждого приложения свой манифест и scope, браузер предлагает установить только ту PWA, на чьей странице человек стоит. Можно: экран «Семья» — ссылки на приложения и, где браузер позволяет, отметка «установлено» (`getInstalledRelatedApps`; работает ли для соседей одного origin — проверить на телефоне)
- **«Дача» — в семью, с ядром** (рекомендация): local-first хозяйство одного дома, свой приватный репозиторий данных — ровно форма семьи; схема участка — модуль приложения, ядро даёт хранилище, синхронизацию, экраны. Её срез для метаприложения — позже, по Я-16…Я-21. Решает человек до первой строки её кода
- **Тотализатор для друзей — не в семью** (рекомендация): общий на несколько людей, а договор семьи — один человек, свой приватный репозиторий, свой токен на устройстве. Друзьям нужны общие ставки, кто что поставил и кто закрыл спор — это общий сервер или бот в общем чате, а не «последняя правка побеждает». Без денег — баллы: денежные ставки — это уже закон об азартных играх
- **Телеграм-бот метаприложения** — реализуем, но это не PWA: нужен исполнитель вне телефона. Самый простой — плановый workflow GitHub Actions по расписанию: читает `summary.json` токеном из секретов, шлёт сообщение через Bot API; сервер не нужен, время отправки выбирается — чего фоновая проверка браузера не умеет. Цена: Телеграм — третья сторона, через него идут числа; бот — **новый названный потребитель** (Я-13, Я-14): что уходит в Телеграм, решается правкой состава, сессией ядра; второй экземпляр токена чтения — в секретах. Бот с командами — уже постоянный сервер или вебхук; не сначала

**Стартовый промпт метаприложения** — заменяет промпт записи «Этап 3, пп. 2–3»:

```
Первая сессия метаприложения семьи — репозиторий TotalUchet
(github.com/goigolden88/TotalUchet, публичный, пока пустой).

Прочитай, только чтение, в C:\Ф\Prognananda\Claude Projs\FamilyCore:
CLAUDE.md; docs/02-Архитектура.md — «Договор семьи», особенно «Срез итогов»
с таблицей «Состав»; docs/05-Решения.md — Я-10…Я-21 и открытые вопросы;
docs/04-Журнал.md — записи 24.09.2026; docs/07-Разведка.md — фон; docs/06-Процесс.md —
как ядро раздаётся подмодулем. Образец настоящего среза — «Делу Время»
(Р-87 «Делу Время», их docs/02-Архитектура.md, «Срез итогов»), читать через
git show в ТаймМенеджмент\DeluVremya. FamilyCore и репозитории приложений
отсюда не правятся: нужна правка договора — говоришь мне, это сессия ядра.

Уже решено ядром, здесь не переобсуждается:
- срез — summary.json в приватном репозитории данных каждого приложения;
  читается fine-grained токеном только на чтение; токен — в settings устройства,
  имена репозиториев — в настройках, не в коде (Я-16); нет файла —
  «срез не отдаёт», не ошибка;
- форма и проверка — core/summary.ts ядра, parseSummary (Я-17, Я-20);
- показываем, а не досчитываем: ни серий, ни средних на день, ни цвета
  по оценке, ни сложения строк разных приложений (Я-15, предварительно);
- through: null — отрезок закончился, но не заморожен; свежесть — computedOn
  и lastEdit; нет пункта «требует внимания» — хозяин не зовёт, а не
  «всё сделано» (Я-21);
- сопоставление строк соседей — по группе, настройкой человека (Я-19).

Сначала — документы, не код, по образцу семьи (skill doc-driven-project):
CLAUDE.md, docs/01…06. Развилки — с рекомендацией, спорное — мне отдельными
вопросами, решения — в момент выбора:
1. берёт ли TotalUchet ядро подмодулем (рекомендация ядра — да: parseSummary
   и типы, каркас стилей, Layout, settings; свой dbName — его вписывает
   в договор сессия ядра);
2. первый экран — недельная сводка: что на нём, порядок, как видны
   свежесть и «не известно»;
3. «требует внимания» — сначала экраном; уведомление вместо четырёх —
   позже, вместе с выключением тем у приложений (Я-18, открытый вопрос);
4. экран «Семья»: ссылки на приложения и отметка «установлено», где браузер
   даёт это узнать; установить соседа одной кнопкой браузер не позволяет —
   проверить на телефоне, что работает;
5. история увиденных срезов — нужна ли и где (Я-17: в срезе её нет);
6. Телеграм-бот — отдельным этапом, после первого экрана: плановый workflow
   GitHub Actions читает срезы и шлёт сводку через Bot API. Бот — новый
   названный потребитель (Я-13, Я-14), Телеграм — третья сторона: что ему
   уходит — правка состава сессией ядра. Сейчас — только записать в План.
План, решения, код — после моего подтверждения. Этап 3 ядра закрывается,
когда первый экран читает настоящий summary.json.
```

**Следующий шаг** — первая сессия из `Claude Projs\TotalUchet` с промптом выше. Параллельно — «Трапеза», «Дневники», «Злотые» по промптам записи «Этап 3, пп. 2–3».

---

## 24.09.2026 — сопровождение: первый настоящий срез — «Делу Время»; Я-21 «как читать срез»

Короткая сессия после «Делу Время» (План, Этап 3, п. 4). Их отчёт: Р-87 «Делу Время», код `c0ffcc2`, ядро `b0047b6`; настоящий `summary.json` из их репозитория данных принят `parseSummary` ядра, суммы групп равны `time.total` во всех отрезках. Сверено отсюда через `git show`: указатель — `b0047b6`, `screens/summary.ts` `settings` не читает, таблица ключей — их `02`, «Срез итогов».

**Решение**

- **Я-21 — как читать срез.** `through: null` — отрезок закончился, а не «заморожен»: прошлое пересчитывается задним числом у всех, свежесть — `computedOn` и `lastEdit`. Пункт «требует внимания» есть — хозяин зовёт, нет — не зовёт, а не «всё сделано». Состав в договоре — семействами ключей со ссылкой на таблицу приложения. Нормы месяца «Делу Время» и строка README давних репозиториев — не добавляются. Выбор человека, рекомендация

**Сделано**

- `05` — Я-21; открытый вопрос «итоги» сужен: осталось три приложения
- `02-Архитектура` — «Состав»: строка «Делу Время» — отдаёт, семейства ключей, свои коды, ссылка; `through` и «нет пункта» — словами Я-21
- `core/summary.ts` — комментарии `through` и `Attention` по Я-21; кода не меняли
- План — п. 4: «Делу Время» отдаёт; п. 5: метаприложение можно начинать

**Находки**

- **Три «не сошлось» «Делу Время» — все о словах договора, не о форме:** окончательность прошлого (Я-21), нормы месяца вне состава (не впрок), README давних репозиториев (цена Я-16, п. 4)
- **`review` только в понедельник** — правило хозяина (Р-41, Р-51 «Делу Время»), законно; отсюда второй пункт Я-21

**Состояние**

*Проверено машинно:* `npm run typecheck` — 0; `npm test` — 0, 380 тестов; CI — в итоге сессии.

*Проверено на настоящих данных* — сессией «Делу Время»: `summary.json` в их репозитории данных, `parseSummary` ядра его принял.

**Следующий шаг**

Метаприложение — новый репозиторий, сессия из его корня, стартовый промпт — запись ниже, «Следующий шаг». «Трапеза», «Дневники», «Злотые» — по своим промптам оттуда же, параллельно; ключи каждого — короткой сессией ядра.

---

## 24.09.2026 — Этап 3, пп. 2–3: срез итогов — место, форма, код в ядре (Я-16…Я-20)

Вход — `07-Разведка` («Что видно для формы среза», пп. 1–9, «Вычитка»), Я-10…Я-15, договор в `02-Архитектура`.

**Решения — все записаны в момент выбора**

- **Я-16 — срез лежит файлом `summary.json` в репозитории данных приложения**, пишет его проход синхронизации ядра; метаприложение читает fine-grained токеном только на чтение. Считается только из синхронизируемых записей и дня расчёта, свежесть — днями `computedOn` и `lastEdit`. Выбор человека из двух, рекомендация; отвергнут общий репозиторий срезов
- **Я-17 — форма общая и описывает себя сама**: договор задаёт форму показателя (`key`, `label`, значение, `basis`), а не перечень; четыре отрезка; «не известно» — кодом и словами; неокончательный итог — `through`; деньги — целыми в минимальных единицах с кодом ISO. Выбор человека из двух, рекомендация
- **Я-18 — «требует внимания» в срезе: закончившиеся дни и состояния**, «сегодня» остаётся напоминаниям приложений; выключение тем у приложений — вместе с уведомлением метаприложения. Выбор человека, рекомендация
- **Я-19 — позиции сессий приложений приняты все пять, промежуточно** (человек: «по ходу реализации уточним»)
- **Я-20 — у «требует внимания» обязательное `basis`**, уточняет Я-18. Нашлось при сборке промптов: Я-19, п. 2 велит «Трапезе» назвать оговорку в основании, а у `Attention` его не было. `format` остаётся 1 — срез ещё никто не писал. Выбор человека, рекомендация

**Сделано**

1. `05` — Я-16…Я-19, открытые вопросы: «итоги» сужен до кода и состава, общее напоминание сужено до выключения тем, Я-19 — промежуточно
2. `02-Архитектура` — «Срез итогов» вместо «строится»: место, форма типами, «требует внимания», чего нет никогда, **первый состав по приложениям — промежуточно**; `summary.json` в раскладке; поле `summary` в `AppConfig`; «Как приложение заводит срез»
3. Код ядра по подтверждению человека:
   - `core/summary.ts` — типы, `SUMMARY_FORMAT`, `UNKNOWN`, `summaryPeriods(day)`, `lastEditOf`, `buildSummary`, `summaryFile`, `checkSummary`, `parseSummary`
   - `AppConfig.summary?` — необязательная функция среза; `checkConfig` не отдаёт хранилищу путь `summary.json`
   - `core/sync.ts` — срез тем же коммитом; функции даются живые записи; упавшая функция или кривая форма — данные уезжают, среза нет, `SyncResult.summaryError`, в журнал ошибок устройства — одна ошибка один раз; `runSync(…, { day })` для тестов
   - `core/layout.ts` — `SUMMARY_PATH` рядом с `META_PATH`, `sortKeys` экспортирован; README со строкой среза — только у приложения со срезом
   - `testing/shelf.ts` — `shelfSummary`, образец функции среза
4. Промпты сессиям четырёх приложений и стартовый промпт метаприложения — ниже, «Следующий шаг»

**Находки по ходу**

- **`readAll` прохода отдаёт надгробия**, а функции итогов приложений рассчитаны на живые записи: функции среза ядро даёт живые, `lastEdit` считает по всем — удаление тоже правка. В плане кода этого не было, сказано человеку до кода
- **Детерминизм среза — механика, а не вкус:** с `settings` устройства или временем расчёта вместо дня два устройства перезаписывали бы срез на каждом проходе. Отсюда Я-19, п. 2 «Трапезы» следует из Я-16 сам
- **Проверка недели строгая, месяца — нет:** Я-12 разрешает «месяц» не календарным, видимым по концам (`periodStartDay` «Злотых»)
- **Тест раскладки без годовых мест** разворачивал `shelf` в конфиг с меньшей таблицей — с полем `summary` компилятор это поймал; в тесте `summary: undefined`

**Состояние**

*Проверено машинно:* `npm run typecheck` — 0; `npm test` — 0, 21 файл, 380 тестов (до сессии 353); CI «Проверка ядра» на `d049c2c` — success; на правке Я-20 — в итоге сессии.

*Не проверено:* срез на настоящем приложении и настоящем GitHub — первым приложением, заведшим срез; `reportError` в журнал ошибок — только в браузере.

**Не описано в документах — на обсуждение**

- **`lastEdit` — местный день устройства.** Устройства в разных поясах в один момент дадут разный день и перезапишут срез друг за другом. Для семьи в одном поясе не бывает; если поедет — `lastEdit` по UTC
- **Старая и новая сборка перезаписывают срез друг за другом** (Я-16, «Цена», п. 2) — лечится обновлением; защита «не переписывать срез новее своей формы» стоит чтения файла каждый проход, не заведена

**Следующий шаг**

1. **«Делу Время»** — сессия из их корня по промпту ниже (человек начинает с неё)
2. **Сессия ядра отсюда, короткая**: ключи, подписи и основания «Делу Время» — в таблицу «Состав» `02-Архитектура` (правка договора, промежуточно по Я-19); разбор «что не сошлось»; первый настоящий `summary.json` на GitHub — проверка `parseSummary` на нём
3. **Метаприложение — сессией из корня нового репозитория**, не отсюда (Я-10: ядро про него не знает; история беседы привязана к папке). Человек заводит пустой репозиторий и клонирует, как FamilyCore 18.09.2026. Стартовый промпт — ниже
4. «Трапеза», «Дневники», «Злотые» — по своим промптам, в любом порядке; каждое приносит ключи в «Состав» сессией ядра

### Промпты сессиям приложений (24.09.2026)

Общее для всех: ядро не раньше `9a956c6`; `src/shared` не правится — противоречие с договором несут сюда; в конце — ключи, подписи, основания для «Состава» и что не сошлось.

**«Делу Время»**

```
Сессия «Делу Время»: срез итогов для метаприложения (ядро: Я-16…Я-20).

Начни по CLAUDE.md. Прочитай src/shared/docs/02-Архитектура.md, раздел
«Срез итогов» — форма, состав, «Как приложение заводит срез», — и
src/shared/docs/05-Решения.md, Я-11…Я-20. src/shared не правится; если
договор мешает — остановись и скажи мне, это сессия ядра.

1. git submodule update --remote src/shared (ядро не раньше 9a956c6),
   npm test, build, smoke — зелёные по коду выхода, коммит указателя.
2. Суммы по группам сейчас склеивает экран (byGroup в Period.tsx,
   TimeDay.tsx, YearTime.tsx) — вынеси в чистую функцию с тестами.
3. Чистая функция summary(data, day) с тестами, отрезки — summaryPeriods(day)
   ядра. Состав (промежуточно, Я-19): минуты по группам и фон отдельно
   (Р-43), основание — «учёт в N днях из M» из elapsedDays; счёты плана
   и факта без текстов; нормы — вердиктом met/failed там, где они есть.
   Идущий отрезок — through = day. Без средних на день, без byKind,
   без текстов пунктов, заметок и повторов (Я-14, Я-15).
   «Требует внимания» — только review: обзор прошлой недели не проведён,
   day — её воскресенье, basis обязателен. Тема day («сегодня») в срез
   не идёт (Я-18).
4. Поле summary в src/app/config.ts. В тестах — checkSummary(buildSummary(...))
   на своих данных.
5. Решение Р-NN «Делу Время»: ключи и подписи среза. Помни: ключ с именем
   группы меняется при переименовании группы (Я-17, «Цена», п. 3).
6. После синхронизации проверь, что summary.json лежит в репозитории
   данных и читается parseSummary.

В конце: список ключей, подписей и оснований — для таблицы «Состав»
ядра; всё, что в договоре не сошлось.
```

**«Трапеза»**

```
Сессия «Трапезы»: срез итогов для метаприложения (ядро: Я-16…Я-20).

Начни по CLAUDE.md. Прочитай src/shared/docs/02-Архитектура.md, раздел
«Срез итогов», и src/shared/docs/05-Решения.md, Я-11…Я-20. src/shared
не правится; если договор мешает — скажи мне, это сессия ядра.

1. git submodule update --remote src/shared (ядро не раньше 9a956c6),
   npm test, build, smoke — зелёные по коду выхода, коммит указателя.
2. Чистая функция summary(data, day) с тестами, отрезки — summaryPeriods(day).
   Недели (промежуточно, Я-19): основание — logged из elapsed; portions,
   records, loose/looseRecords, assumed; ккал с основанием KcalSum
   и среднее на день учёта; нормы — вердиктом met/failed/open,
   в основании — правило minDays/maxDays. Названия групп и категорий —
   только если решишь, что они в составе (Я-14); Intake.note и рецепт —
   никогда.
   Месяцы — metrics: { unknown: 'not-provided', text }: месяц — новый расчёт
   и твоё отдельное решение, не сейчас (Я-19, п. 1).
3. «Требует внимания» — вчерашние пропуски приёмов только по записям,
   без отметок «не было» (они в settings, срез их не видит, Я-16):
   count — число пропущенных приёмов, day — вчера, basis — «по записям;
   отметки „не было“ не учтены» (Я-19, п. 2; Я-20). «Сегодня пусто»
   остаётся твоему напоминанию.
4. Поле summary в src/app/config.ts; в тестах — checkSummary(buildSummary(...)).
5. Решение Р-NN «Трапезы»: ключи, подписи, основания среза.
6. После синхронизации — summary.json в репозитории данных, читается parseSummary.

В конце: ключи, подписи, основания — для таблицы «Состав» ядра; что
в договоре не сошлось.
```

**«Дневники»**

```
Сессия «Дневников»: срез итогов для метаприложения (ядро: Я-16…Я-20).

Начни по CLAUDE.md. Прочитай src/shared/docs/02-Архитектура.md, раздел
«Срез итогов», и src/shared/docs/05-Решения.md, Я-11…Я-20 — особенно Я-19,
пп. 3–4: это ваши позиции, приняты промежуточно. src/shared не правится.

1. git submodule update --remote src/shared (ядро не раньше 9a956c6),
   прогон зелёный, коммит указателя.
2. Новые расчёты, каждый — своим решением Р-NN «Дневников», чистой
   функцией с тестами:
   - дни болезни в промежутке — пересечение эпизодов с промежутком,
     наложения склеены, открытый длится до дня расчёта;
   - contentStats по Period ядра — начато, досмотрено, брошено; «досмотрено» —
     нынешний статус начатого в промежутке; плюс «ещё N с точностью до
     месяца»; месячную дату — никогда первым числом.
3. Чистая функция summary(data, day), отрезки — summaryPeriods(day).
   Состав (промежуточно): дни болезни и счёт эпизодов — без названий,
   симптомов, заметок (ступень 2, потребитель — метаприложение);
   тренировки — счёт, минуты и км с оговоркой в основании; контент по
   промежутку, неделя — { unknown: 'month-precision', text }.
   Измерения — не в срез. Никогда: название эпизода, note, симптомы,
   comment.
4. «Требует внимания»: открыт ли эпизод (day — день начала), число
   просроченного в циклах, число зависшего в «смотрю» — всё с basis (Я-20),
   без названий позиций.
5. Цены отметок циклов в срез пока не идут: их не просит ни один
   потребитель. Когда попросят — целым в копейках с 'RUB' (Я-17).
6. Поле summary в конфиге; checkSummary(buildSummary(...)) в тестах;
   после синхронизации — summary.json в репозитории данных.

В конце: ключи, подписи, основания — для таблицы «Состав» ядра; что не сошлось.
```

**«Злотые»**

```
Сессия «Злотых»: срез итогов для метаприложения (ядро: Я-16…Я-20).

Начни по CLAUDE.md. Прочитай src/shared/docs/02-Архитектура.md, раздел
«Срез итогов», и src/shared/docs/05-Решения.md, Я-11…Я-20 — особенно Я-13:
ваши три ступени. src/shared не правится.

1. git submodule update --remote src/shared (ядро не раньше 9a956c6),
   прогон зелёный, коммит указателя.
2. Чистая функция summary(data, day) с тестами, отрезки — summaryPeriods(day).
   Сейчас только ступень 1 Я-13, без сумм: есть ли записи за месяц;
   through у отрезка месяца — по какое число доведены выписки
   (recordedThrough); сколько регулярных ждёт; день последнего снимка
   капитала. Недели — { unknown: 'not-provided', text }.
   Ступени 2 и 3 не просит ни один потребитель — не отдавать.
   Имена людей — никогда.
3. Если ваш «месяц» с periodStartDay не календарный — отдай его концы
   как есть: from/to видны, ядро месяц не проверяет (Я-12).
4. «Требует внимания»: за прошлый месяц нет записей, регулярные ждут —
   с basis (Я-20).
5. Склейку итога месяца из Month.tsx в чистую функцию выносить сейчас
   не нужно: она понадобится для ступеней 2–3. Запиши это в своём Плане.
6. Поле summary в конфиге; checkSummary(buildSummary(...)) в тестах;
   после синхронизации — summary.json в репозитории данных.

В конце: ключи, подписи, основания — для таблицы «Состав» ядра; что не сошлось.
```

### Стартовый промпт метаприложения — первая сессия из корня его репозитория

Имя репозитория и приложения выбирает человек до первой сессии. Решения метаприложения — его «Р-NN» в его `docs/`, не здесь.

```
Первая сессия метаприложения семьи. Репозиторий пустой, склонирован сюда.

Прочитай, только чтение, в C:\Ф\Prognananda\Claude Projs\FamilyCore:
CLAUDE.md; docs/02-Архитектура.md — «Договор семьи», особенно «Срез итогов»;
docs/05-Решения.md — Я-10…Я-20 и открытые вопросы; docs/07-Разведка.md —
фон: что приложения умеют и чего не отдают; docs/06-Процесс.md — как ядро
раздаётся подмодулем. FamilyCore и репозитории приложений отсюда не правятся:
нужна правка договора — говоришь мне, это сессия ядра.

Что уже решено ядром и здесь не переобсуждается: метаприложение читает
summary.json из приватных репозиториев данных приложений fine-grained
токеном только на чтение, токен — в своих settings устройства, имена
репозиториев — в настройках, не в коде (Я-16); показывает, а не досчитывает:
ни серий, ни средних на день, ни цвета по оценке, ни сложения строк разных
приложений (Я-15, предварительно); сопоставление строк соседей — по группе,
настройкой человека (Я-19); нет файла — «срез не отдаёт», не ошибка.
Форма и проверка — core/summary.ts ядра (parseSummary).

Сначала — не код, а документы по образцу семьи (doc-driven-project):
CLAUDE.md, docs/01…06. Развилки — с рекомендацией, мне отдельными вопросами:
1. берёт ли метаприложение ядро подмодулем (рекомендация сессии ядра — да:
   parseSummary и типы среза, каркас стилей, Layout, settings; dbName свой);
2. первый экран — недельная сводка: что на нём, в каком порядке, как
   видна свежесть (computedOn, lastEdit) и «не известно»;
3. «требует внимания» — только экраном; уведомление вместо четырёх —
   позже, вместе с выключением тем у приложений (Я-18, открытый вопрос);
4. как хранить увиденные срезы, если нужна история (Я-17: в срезе её нет).
План, решения — в момент выбора, код — после моего подтверждения.
Этап 3 ядра закрывается, когда первый экран читает настоящий summary.json.
```

---

## 24.09.2026 — Метаприложение: планирование начато (Я-10, Я-11); Этап 3 — договор итогов

Повод — записка сессии «Дневников» о том, что метаприложению можно у них взять (данные, чистые функции итогов, темы напоминаний, правила показа чисел, риски). Записка — материал разведки, в репозиторий не положена: её восемь пунктов стали анкетой Этапа 3.

**Решения**

- **Я-10** — отсрочка Р-51 «Трапезы» снята для договора, не для аналитики. Здесь — только договорная часть; экраны метаприложения — его репозиторий. Разведка — отсюда одной анкетой, сводки — на вычитку сессиям приложений
- **Я-11** — итоги считает приложение своими чистыми функциями, метаприложение читает готовый срез. Где срез лежит и какой он формы — после разведки

**Проверено, что разведку можно вести отсюда:** все четыре репозитория читаются через `git show HEAD` — `Еда/Trapeza` на `4a9a8bb`, `ТаймМенеджмент/DeluVremya` на `ed95582`, `Дневники/Dnevniki` на `fba632a`, `Злотые` на `fd9c68f`.

**Не описано в документах — на обсуждение**

- **Общее напоминание через метаприложение** — мысль человека: четыре уведомления в одно время засоряют экран. Записано открытым вопросом в `05`, решается вместе с формой среза

**Состояние.** Код не менялся; `01`, `02`, План, `05` — под Я-10 и Я-11.

**Разведка собрана** — `07-Разведка.md`: все четыре приложения по восьми пунктам, одной таблицей на пункт. Записка «Дневников» сверена с их кодом: `contentStats` считает по году, а не за промежуток; у здоровья свой `Period` с необязательными концами — не `Period` ядра. Остальное сошлось.

**Главное из разведки**

- **«Число с основанием» — у всех четырёх, пришли к нему независимо** (Р-01 «Трапезы», Р-38 «Дневников», Р-07 «Злотых», основания в подписях «Делу Время»). Первый кандидат в форму среза
- **Зерно итогов разное:** день, неделя, любой промежуток, месяц, год, «на дату». Общего отрезка нет
- **Три темы напоминаний из восьми зависят от настроек устройства,** которые не синхронизируются: «не было» у «Трапезы», имя экрана у «Делу Время», день прошлого вопроса «Ещё смотришь?» у «Дневников». Для общего напоминания через метаприложение это прямое возражение: срез одного устройства у другого скажет иное
- **Недельный обзор «Делу Время» уже описал, чего ждёт** — таблица в их Плане, «Отложено» (Р-50 «Делу Время»)

**Вычитка «Злотых»** (на `be62dab`) — внесена в `07`: поправки в таблицах, новое — в разделе «Вычитка». Выборочно сверено с их кодом — `savedProblem`, `fullCapitalOn`, `debtsOn`, склейка в `Month.tsx`, пороги `OVER_USUAL_MIN`, `GROWTH_MIN` — сошлось. Для формы среза из неё следуют четыре требования: код валюты у каждого денежного числа; «не известно» — с причиной, а не голым `null`; отрезок — явными `from`/`to`; «неокончательный итог» и отметка «по записям по ДД.ММ» сверх времени расчёта. Деньги в срезе — три ступени по потребителю, решает человек; имена людей из долгов — никогда. Для недельного обзора «Делу Время» деньги на недельном зерне почти всегда «не известно».

**Находки «Злотых» для самих «Злотых»** — не ядро, говорятся человеку: `periodStartDay` сохраняется экраном, но ни один расчёт его не читает; `01-Проект` «Злотых» ссылается на Р-51 «Трапезы» без Я-10; склейку «Месяца» вынести в `src/summary/`, когда решат форму среза.

**Вычитка «Делу Время»** (на `ed95582`) — внесена в `07`. Сверено с их кодом: тема `day` говорит по одному условию — есть ли блоки за сегодня (`remind.ts`), имя экрана — только в тексте; `checkNorm` — только `met: boolean`. **Поправка к записи выше:** от настроек устройства зависят две темы из восьми, а не три; у «Делу Время» не запись — идущий таймер. Новое для среза: вид категории — не оценка, красить по нему нельзя; сопоставление с соседями — по группе, настройкой человека у потребителя; тексты заметок не идут; серий и «в среднем в день» нет.

**Решения:** **Я-12** — первый потребитель среза — метаприложение, обзор «Делу Время» даёт ссылку; зерно — неделя с понедельника и календарный месяц. Уточняет порядок Я-10 новым номером — противоречие нашла их вычитка. **Я-13** — деньги в срезе тремя ступенями по потребителю, имена людей — никогда. Оба — выбор человека. **Я-14** — свободный текст в срез не идёт, названия — только названному потребителю. **Я-15** — потребитель показывает, а не досчитывает и не оценивает; **предварительно**, человек: пересмотреть по ходу работы над метаприложением и на настоящих данных — в открытых вопросах `05`.

**Вычитка «Трапезы»** (на `4a9a8bb`) — внесена в `07`. Сверено с их кодом: тема `day` — «или»: вчерашний пропуск основного приёма либо пустое сегодня (`remind.ts`); `summarize` берёт `ReadonlyMap` блюд и сама отдаёт «без категории»; неделя без записей не идёт ни в счёт нормы, ни в `open`; `Period` — ядра. Главное для среза: день без записей — неизвестен, а не ноль; прошлые недели меняются правкой справочников без новых записей — нужна отметка «посчитано ДД.ММ»; итогов месяца у «Трапезы» нет — календарный месяц Я-12 для неё новый расчёт. Решений за «Трапезу» не записано: месяц и тема `day` без отметок «не было» — её позиции, материал к форме среза, решает человек.

**Вычитка «Дневников»** (на `b8ea708`, код тот же, что на `fba632a`) — внесена в `07`; разведка вычитана всеми четырьмя. Сверено с их кодом: `Spent { sum, priced, marks }`; эпизод в промежутке — по дате начала, не по пересечению; у `activityTotals` минуты без основания. Главное для среза: функции «дни болезни в промежутке» нет — первый факт обзора «Делу Время» требует нового расчёта «Дневников»; у цены отметки нет кода валюты, и одна трата бывает и в «Дневниках», и в «Злотых»; тексты тем `illness` и `overdue` несут диагноз и названия. От устройства тема «Ещё смотришь?» зависит только временем вопроса. Позиции сессии — ступени здоровья и `contentStats` по промежутку — материал к форме, решает человек.

**Договор: что приложения соблюдают уже сейчас.** По просьбе человека — приложения будут дописываться, пока строится метаприложение, и не должны разойтись с решённым. В `02-Архитектура`, «Договор семьи», раздел «Чего в договоре нет» стал «Срез итогов — строится»: список правил Я-11…Я-15 для новых функций и правок приложений. Правка документа, не договора: ничего нового не решено, решённое сведено в одно место, куда приложения и так ссылаются. В репозитории приложений отсюда не писалось (`CLAUDE.md`, «Чего здесь не делается») — человеку дан промпт для их сессий.

**Состояние.** Код не менялся. Документы: `02`, `03`, `05` (Я-10…Я-15), `07`, этот Журнал.

**Не описано в документах — на обсуждение**

- Находки вычиток для самих приложений — говорятся человеку, уходят их сессиям: у «Злотых» `periodStartDay` не читает ни один расчёт; у «Делу Время» устарела таблица «Отложено»; у «Дневников» `Period` здоровья — наследство, «Траты» за период (их Р-36) не сделаны, у `activityTotals` нет основания

**Следующий шаг — новая сессия: решение о месте, пути и форме среза** (План, Этап 3, п. 2 — там же список входа). Начать с `07-Разведка`, «Что видно для формы среза», и открытых вопросов `05` (общее напоминание; Я-15 — пересмотреть на деле).

---

## 24.09.2026 — «Дневники» переведены на `897f3ae`: семья на ядре целиком

**Запись сделана не отсюда**, а сессией перевода «Дневников» из их корня,
по прямому разрешению человека — как 19.09.2026. Тронут только этот Журнал:
код, решения, План и Архитектура ядра — нет.

**Перевод «Дневников» — последний в семье** (Р-81…Р-86 «Дневников»;
коммит-переезд `bf53b52`, в `main` — `fba632a`, деплой — success;
телефон — за человеком). Теперь на ядре все четыре приложения.

- `<CoreProvider value={{ config, db, sync }}>` принял `db` и `sync`
  без `as`; правок ядра не понадобилось
- **Годовая нарезка Я-09 сошлась на настоящих данных:** слепок собран из
  самого репозитория данных «Дневников» (клон только для чтения), и
  `buildFiles` ядра дал 11 файлов с теми же отпечатками, что в его дереве, —
  11 из 11; прежний код на том же слепке — те же байты
- `tsc` с `src/shared` — 0; своих тестов 390 (было 629); прогон — 80;
  промпт импорта, манифест и иконки через `writeIcons` — байт в байт прежние;
  сверка вёрстки двух сборок — восемь экранов из девяти совпали целиком,
  в «Настройках» — блок импорта ядра

**Находки — ядро не правилось, на обсуждение**

- **`REMINDER_TAG` зашит как `remind`.** У «Дневников» фоновая проверка
  была заведена под `overdue` (их Р-54), и работник ядра её не слушает —
  напоминания замолчали бы. Параметр ядра отвергнут как «ради одного
  приложения»: «Дневники» включают напоминания заново, строкой «Что нового»,
  и снимают прежнюю регистрацию сами (их Р-85). Других приложений со старым
  именем нет
- **Тема напоминания не знает, что проверка ручная** (`createReminders`):
  «Проверить сейчас» у «Дневников» больше не спрашивает «Ещё смотришь?»
  чаще раза в неделю. Принято; понадобится — параметр `force` в `topics`
- **Каркас стилей: `.row input { min-width: 0 }` и `.row select { flex: 1 }`**
  меняют вид полей в рядах у приложения не из линии «Делу Время»: поле даты
  рядом с «сегодня» сжималось до 54 px. «Дневники» вернули прежнее своими
  правилами; у «Делу Время» и «Трапезы» каркас — их собственные правила
- **Слепок из репозитория данных вместо файла-копии** — сильнее: свежий по
  определению и сверяет раскладку с настоящим деревом. Скрипт и сверка
  вёрстки лежат в папке сессии; брать ли их в ядро — вопрос вместе с
  прогоном (План ядра, «Отложено»)

**Следующий шаг** — у ядра переводов больше нет: сопровождение по находкам
приложений; «Итоги» в договоре — когда планируется метаприложение.

---

## 24.09.2026 — Сопровождение перед переводом «Дневников»: годовая нарезка (Я-09); лента — не ядро; чеклист

Третья сессия сопровождения — по промпту из `06-Процесс` «Делу Время», находки перевода «Делу Время». «Дневники» сверены через `git show a913dcb`, приложения — через `git show HEAD`.

**Находка 1 — годовой нарезки нет. Закрыта, Я-09, правка договора**

- **Воспроизведено на «Полке»:** новое хранилище `reviews` — по годам, дата бывает днём, месяцем и `null`. До правки 9 проверок раскладки и синхронизации падали, `typecheck` — на `split: 'year'`
- **Что это** — правка договора семьи, а не механика: `Place` и «Раскладка репозитория данных» знали два вида. Отвергнуто переложить данные «Дневников» по месяцам — это миграция в переводе, который их не трогает
- **Сделано:** `Place` — `split: 'month' | 'year'`; `<dir>/ГГГГ.json`, год — первые четыре знака даты дня или месяца; без даты и с кривой — `undated.json`. `storeOf` узнаёт в папке только файлы её нарезки. README: строка `<dir>/ГГГГ.json`; «Прошлые годы» или «месяцы и годы» — только если годовые места есть
- **Правило в договоре:** нарезка места после первого релиза не меняется — иначе прежние файлы станут чужими, а записи из них не дойдут до свежего устройства
- **С «Дневниками» на `a913dcb` расхождение одно:** год у них — число из `yearOf`, у ядра — четыре знака. Для лет 1000…9999 файл тот же побайтно; год до 1000 у них дал бы файл, который их же `storeOf` не узнаёт
- **Остальной путь проверен:** слияние и `merged` идут через `storeOf` и не менялись; проход синхронизации — годовой файл с сервера прилетает, своё уезжает в год, переезд между годами опустошает старый файл; слепок раскладки не знает — круг через файл с годовым хранилищем

**Находка 2 — лента: без даты вниз у ядра, наверх у «Дневников». Не правится**

Параметр порядка задал бы по-своему только один потребитель — правило границы. И он не нужен: `groupFeed` отдаёт группу без даты последней, а экран ленты — приложения; поставить её первой — одна строка в их `Feed.tsx`. Выбор — сессии перевода «Дневников»: принять порядок ядра (строка «Что нового») или держать свой на экране. Кроме порядка, ядро отличается от них текстом: заголовок «Без даты» вместо «Дата не читается», прочерк вместо «нет даты» — тоже решаемо на их экране.

**Находка 3 — чеклист «Как приложение подключает ядро». Только документ**

П. 6 — `isEmptyBase(counts, own)` тем же списком, что `useFirstRun(own)` (у «Делу Время» — `OWN_STORES`, не все хранилища; сверено в их коде); реестр и разделы импорта — `Snapshot<StoreRecord>['data']`, `ImportPlan<StoreRecord>`; подпись вида — своей `kindLabel`, порядок ленты — ядра. **`share_target` — в п. 11**, а не в п. 6: это поле манифеста через `manifest` у `familyVite`. П. 3 — место по годам с той же папкой и датой.

**Коммиты:** `9701be7` — Я-09 и проверки, падавшие до правки; `7743aef` — код, договор, чеклист, План.

**Состояние**

*Проверено машинно:*
- `npm run typecheck` — 0; `npm test` — 0, 20 файлов, 353 теста
- **Приложения без годовых мест — побайтно прежние.** На «Полке» без `reviews`: файлы, README и `storeOf` закреплены снимком, снятым на `98da218` до правки, — зелёный до и после
- **На настоящих приложениях, без правки их репозиториев:** `git archive HEAD` в папку сессии, рабочая копия ядра — в `src/shared`, ядро `98da218` — рядом, `node_modules` — **копией** (урок 23.09.2026), тесты — `--cache=false`. «Трапеза» `2622d52` — `tsc` 0, 202 теста; «Делу Время» `afeae32` — `tsc` 0, 450; «Злотые» `c05cc43` — `tsc` 0, 632. Сверка старого и нового ядра на их конфигах: README побайтно, `storeOf` на 14 / 20 / 65 путях, файлы пустой базы — совпали. Их рабочие копии чистые, `node_modules` не тронуты
- CI «Проверка ядра» на `7743aef` — success

*Не проверено:* сборка и прогон приложений с новым ядром — их сессиями при обновлении указателя; годовая нарезка на настоящих данных «Дневников» — их сессией перевода (проверка — в их промпте).

**Ошибки по ходу, мои**

- **Сверка, зелёная впустую.** В файле сверки путь к старому ядру был на уровень короче — `tsc` копии назвал модуль ненайденным, а `vitest` отчитался зелёным: файл сверки ушёл в «skipped», а не в падение, и код выхода остался 0 (видно по счёту: пропущенных столько, сколько проверок сверки). Путь поправлен, в сверку добавлена проверка, что сравниваются два разных ядра, прогон повторён. Впредь: сверка двух версий начинается с проверки, что версий две, а итог читается и по «skipped», не только по коду выхода
- `cat > файл` без ввода повис на stdin две минуты — остановлен, ничего не записал

**Не описано в документах — на обсуждение**

- **`checkConfig` не видит двух мест с одной папкой.** Два хранилища в одной `dir` — или месячное и годовое, или два месячных — перемешали бы файлы: `storeOf` отдал бы путь первому. Сейчас такого нет ни у кого; проверка дешёвая, но это правка API, которую никто не просил
- Прежние — открыты: предупреждение Р-21 «Трапезы»; импорт, который удаляет

**Релиз Я-09 — сделан отсюда.** По прямому разрешению человека указатель обновлён этой сессией в трёх приложениях — исключение из «другие репозитории только читаются», как 19.09.2026. В каждом — только `src/shared`, отдельным коммитом, после `npm test`, `build` и `smoke` с кодом 0:

| Приложение | Коммит указателя | Тесты | Прогон | Деплой |
|---|---|---|---|---|
| «Трапеза» | `4a9a8bb` | 203 | 96 | success |
| «Злотые» | `fd9c68f` | 629 | 166 | success |
| «Делу Время» | `dfd05e3` | 449 | 144 | success |

Журналы приложений не тронуты — строка для них у человека. Телефоны — не проверялись: видимого изменения нет.

**Следующий шаг**

Перевод «Дневников» — сессией из их корня; промпт — в `06-Процесс` «Делу Время», поправленный полный текст — у человека.

---

## 24.09.2026 — «Делу Время» переведено на `d12def9`; для «Дневников» ядру не хватает годовой нарезки

**Запись сделана не отсюда**, а сессией перевода «Делу Время» из его корня, по прямому разрешению человека — как 19.09.2026. Тронут только этот Журнал: код, решения, План и Архитектура ядра — нет.

**Перевод «Делу Время» — третий на ядре** (Р-83…Р-85 «Делу Время»; коммит-переезд `1b5dcb6`, в `main` — `cd30231`, деплой — success; телефон — за человеком).

- `<CoreProvider value={{ config, db, sync }}>` принял `db` и `sync` без `as`; правок ядра не понадобилось. Починка `SharedDb` (`d1f282f`) сработала и у второго приложения
- `tsc` с `src/shared` — 0; своих тестов 449 (было 722); прогон — 144 проверки; манифест по значениям прежний — `share_target` впервые идёт через `manifest` у `familyVite`; промпт импорта — байт в байт прежний; иконки через `writeIcons` — байт в байт
- Каркас стилей — 71 правило, свои — 95, вместе ровно прежние 166; сверка вёрстки двух сборок: десять экранов из одиннадцати совпали целиком, в «Настройках» — только два абзаца импорта вместо одного (Я-07)
- **Для чеклиста «Как приложение подключает ядро», п. 6** — не описано, найдено переводом: `isEmptyBase(counts, own)` рядом с `useFirstRun(own)`; реестр и разделы импорта приложения — по таблице хранилищ, `Snapshot<R>['data']` и `ImportPlan<R>`; подпись вида ленты — своей `kindLabel`

**Находка 1 — блокирует перевод «Дневников»: годовой нарезки в ядре нет.** Сверено через `git show a913dcb` «Дневников»: `cycleEvents` и `content` лежат по годам — `Place` с `split: 'year'`, `cycles/ГГГГ.json`, `content/ГГГГ.json`, без даты — `undated.json`; год — `yearOf`, принимающий и день, и месяц `ГГГГ-ММ`. `Place` ядра и договор семьи знают только `none` и `month`. Без изменения данных «Дневники» не перевести — нужна правка договора, решение Я-NN и релиз на всех.

**Находка 2 — поведение ленты.** Ядро ставит запись без даты вниз (Р-61 «Делу Время»), «Дневники» — наверх, как поломку. Параметр ядра или «Дневники» принимают — решить.

**Следующий шаг** — сессия сопровождения по этим находкам **до** перевода «Дневников». Промпт — в `06-Процесс` «Делу Время», «Передача», «Сопровождение `FamilyCore` перед переводом «Дневников»».

---

## 23.09.2026 — Сопровождение: находки «Злотых» — исходы импорта, сводка по готовому тексту, выборка по индексу

Вторая сессия сопровождения — по промпту из `06-Процесс` «Злотых». Три находки, все сверены с «Злотыми» через `git show` на `4b303ed`. Решения — **Я-07** и **Я-08**, выбор человека по рекомендации.

**Находка 1 — у `ImportPlan` два исхода, а у импорта «Злотых» их пять. Закрыта, Я-07, API расширен**

- **Повод в промпте устарел, беда — нет.** «Заметки о склейке переводов» больше нет (Р-19 «Злотых»). В `issues` вместо отказов лежат «округление» (учёт и капитал) и «итог периода заменён операциями»
- **Шире, чем в промпте:** импорт «Злотых» не только правит счета (`loadedThrough`, Р-26 «Злотых»), но и удаляет — итоги периодов уходят надгробием через `writes` (Р-02 «Злотых»). Сводка звала это отказом, «Загрузить N» считал надгробия вместе с новым, а общий абзац ядра обещал «импорт только добавляет»
- **Договор семьи не затронут:** `ImportPlan` не лежит ни в базе, ни в репозитории данных. Это API ядра — решение Я-07, релиз не обязателен всем сразу. Меняет правило Р-60 «Дневников» «импорт только добавляет»
- **Сделано:** `notes?`, `changed?`, `removed?` у `ImportPlan`; `Count` (прежний `Added` — синоним); `mergeResults` сводит новые поля; `countsText`. Сводка: Добавится → Изменится → Удалится → Уже есть → «Загрузится, но стоит посмотреть» → «Не разобрано». Если приложение не назвало ничего, а записи есть, сводка говорит «Запишется записей: N». Общий абзац — правда для любого приложения

**Находка 2 — `ImportRecords` не принимает готовый текст. Закрыта, Я-07**

Сводка и запись вынесены в `ImportSummary({ text, planImport, onApplied, onCancel })`. `ImportRecords` пользуется ею сам: номер разбора служит ключом, поэтому тот же текст разбирается заново по свежему слепку. Что сказать после записи, решает место, где сводка стоит, — через `onApplied(plan)`. Отвергнут пропс `initialText`: он тянул весь экран импорта на «Капитал».

**Находка 3 — индексы создаются, а читать по ним нельзя. Закрыта, Я-08**

- `db.getByIndex(store, index, ключ | { from, to }, { includeDeleted })`. `IDBKeyRange` не выходит из `core/db.ts`; индекс ищется в `indexNames` устройства; нет индекса — ошибка с именами
- **Попутная ловушка:** `createStores` заводит индексы конфига только на свежей базе и только у `v1Stores`. Индекс, дописанный в конфиг после релиза, у давней установки не появится, а миграция, заводящая его же, упадёт на свежей базе. Правило «`indexes` хранилищ версии 1 заморожен» записано в Я-08, в комментарий `AppConfig.indexes` и в `02-Архитектура`. Касается будущих индексов долгов «Злотых» (Р-32 «Злотых», «Цена», п. 3): только миграцией, в конфиг не дописывать

**Коммиты:** `05d801e` — решения и проверки, падавшие до правки (11 тестов); `1e092b0` — код и Архитектура.

**Состояние**

*Проверено машинно:*
- `npm run typecheck` — 0; `npm test` — 0, 20 файлов, 338 тестов
- **Совместимость — на настоящих приложениях, без правки их репозиториев:** `git archive HEAD` «Трапезы» (`cf2cfbc`) и «Злотых» (`4b303ed`) в папку сессии, ядро рабочей копии — в их `src/shared`, `node_modules` — ссылкой. «Трапеза» — `tsc` 0, 199 тестов; «Злотые» — `tsc` 0, 621 тест. Правок кода при обновлении указателя не требуется ни одной
- CI «Проверка ядра» на `1e092b0` — success

*Не проверено:* сборка и прогон приложений с новым ядром (`vite build`, `smoke`) — их сессиями. Новая сводка на экране глазами не видена.

**Ошибка по ходу, моя.** При проверке совместимости vitest переписал свой кеш результатов (`node_modules/.vite/vitest/…/results.json`) в `node_modules` «Трапезы» и «Злотых» — через ссылку. В git это не входит, `git status` обоих чистый, но запись в чужую папку была. Впредь для такой проверки — `vitest run --cache=false` или копия `node_modules`, а не ссылка.

**Не описано в документах — на обсуждение**

- **Импорт, который удаляет.** Ядро теперь умеет назвать удаление, но не решает, вправе ли импорт удалять вообще: это решили «Злотые» (Р-02 «Злотых»). Если второе приложение захочет того же — вопрос в договор ли это, а не только в сводку
- **Предупреждение Р-21 «Трапезы»** (18.09.2026) — по-прежнему открыто; общий абзац переписан, но это предупреждение туда не вошло

**Следующий шаг**

Сессии из корней приложений: «Злотые» — перевод своих исходов на новые поля и `FetchRates` на `ImportSummary`; «Трапеза» — только обновление указателя. Промпты — у человека.

---

## 19.09.2026 — Документы: «Финансы» стали «Злотыми»; `zloty` в перечне баз договора

**Правка сделана не отсюда**, а сессией планирования «Злотых» из их корня — исключением из правила «другие репозитории только читаются», по прямому разрешению человека. Только документы, код ядра не тронут; решения Я-NN нет: правило «`dbName` у каждого приложения свой» не меняется, в перечень добавлено четвёртое имя.

**Сделано**

- «Финансы» → **«Злотые»** (`goigolden88/Zloty`, Р-10 «Злотых») там, где речь о настоящем и будущем: `CLAUDE.md`, `01-Ядро` («Кто берёт ядро»), `03-План` («Дальше»), промпт передачи в `06-Процесс`. «„Финансы“ стали бы четвёртой копией» в «Проблеме» `01` — история, не тронута
- Договор семьи, «Устройство и origin»: `dbName` четвёртого приложения — `zloty`

**Что спланировано у «Злотых»** — Р-01…Р-11 «Злотых», 18–19.09.2026. Ядро берётся подмодулем с первого коммита кода, деньги — не ядро (Р-03 «Злотых»). «Злотые» — первое приложение, которое начинается на ядре, а не переезжает: чеклист «Как приложение подключает ядро» писался под переезд, их адаптация — в их `02-Архитектура`.

**Нехватки ядра для их Этапов 0–1 не найдено.** Несколько экранов импорта (свой в каждом модуле) — через пропсы `ImportRecords` и `buildPrompt(specs)`. Возможная находка их Этапа 0: `about.sources` — одна строка на приложение, а экранов импорта будет несколько; правки не требуется, пока одна строка покрывает все.

**Состояние.** Код не менялся, проверять нечего; CI «Проверка ядра» запустится на пуш документов.

**Следующий шаг** — прежний: сессия сопровождения по находкам приложений; Этап 0 «Злотых» может принести свои.

---

## 18.09.2026 — Сопровождение: находки перевода «Трапезы»; `db` приложения подходит под контекст

Первая сессия сопровождения — по передаче Б2 «Трапезы» (ветка `familycore` «Трапезы», подмодуль на `981200e`). Три находки; одна блокировала перевод. В `src/shared` «Трапезы» ничего не правилось. Перевод «Трапезы» лежит незакоммиченным в её рабочей копии, поэтому её употребления читались с диска — только поиском, не сравнением.

**Находка 1 — `db` приложения не подходил под `SharedDb`. Закрыта, `d1f282f`, API не менялся**

- **Воспроизведено на «Полке»** пробным файлом: `createDb(shelf)` в `SharedDb` — та же ошибка, что у `tsc` «Трапезы»: `ChangeEvent<ShelfStores>` не подходит под `ChangeEvent`. `createSync(shelf, db)` в `Sync` и `shelf` в `SharedConfig` подходили
- **Что это** — ошибка типов ядра, не шва. По полям событие приложения под общее подходит: `ChangeEvent<StoreMap>.store` — `string`. Но компилятор сравнивает обобщённый `ChangeEvent<R>` по параметру и из-за `keyof R` требует `StoreMap` не уже таблицы приложения. Я-03 хотел ровно того, что написано в `ui/core.tsx`, — типы без таблицы хранилищ; `ChangeEvent` с умолчанием эту цель не выполнил, а тест, кладущий настоящий `createDb` в `SharedDb`, в Этапе 1 не написан
- **Починка** — в `SharedDb.onChange` событие описано без обобщения: `{ store: string; origin; count }`. `ChangeEvent<R>` в `core/db.ts` не тронут: приложение по-прежнему видит свои имена хранилищ в своих подписчиках. Отвергнуто: `ChangeEvent.store: string` у всех — смена API ради того же итога, приложения теряют проверку имён
- **Решения Я-NN нет:** общий интерфейс стал принимать шире, API приложений тот же (`06-Процесс`, передача, п. 3)
- **Проверка до правки** — `ui/core.test.ts`: `{ config: shelf, db, sync } satisfies Core`, отдельным коммитом `c734e0b`. До правки `npm run typecheck` — код 1 с той же ошибкой; после — 0

**Находка 2 — `useFeed` отдаёт `FeedItem` со строковым `kind`. Не правится**

Строка — правильная граница. `FeedItem.kind` — строка по договору семьи; `compareFeed`, `groupFeed`, `filterFeed` берут `FeedItem` со строкой; `feedItems` «Трапезы» и так отдаёт `FeedItem[]`. Параметр вида сузил бы только подпись вида, а ей всё равно подписывать незнакомый вид как есть. `kindLabel(kind: string)` «Трапезы» — верная форма.

**Находка 3 — `ImportRecords` ставит `intro` только перед общим абзацем. Не правится**

Порядок «своё — потом общее» правильный: `intro` — слова приложения, что сюда загружают; общий абзац — механика, одинаковая у всех. Слот «после» появился бы ради одной фразы одного приложения — правило границы. У «Трапезы» абзац Р-21 «Трапезы» теперь стоит над «Файл готовится по промпту…», текст тот же.

**Непереведённые копии.** У «Делу Время» на `d86f0aa` и «Дневников» на `a913dcb` (сверено через `git show`) `db` — одиночка модуля, `ChangeEvent` не обобщён (`store: SyncedStore`), `useFeed` импортирует `db` напрямую. Контекста и `SharedDb` нет — беды нет.

**Состояние**

*Проверено машинно:* `npm run typecheck` — 0; `npm test` — 0, 20 файлов, 326 тестов; CI «Проверка ядра» — в итоге сессии.

*Не проверено:* `tsc` «Трапезы» без приведений после обновления указателя — хвост в Плане.

**Не описано в документах — на обсуждение**

- **Предупреждение Р-21 «Трапезы»** («записи загружай на одном устройстве — остальные получат синхронизацией») верно, вероятно, для любого приложения, чей импорт даёт записям новые случайные id. У «Делу Время» и «Дневников» его нет. Если оно общее — место ему в общем абзаце `ImportRecords`, и тогда это правка ядра для всех, а не слот

**Следующий шаг**

Сессия «Трапезы»: `git submodule update --remote src/shared`, `tsc` без приведений, дальше перевод по Б2.

---

## 18.09.2026 — Этап 1: ядро заведено и собирается без приложения; код написан, этап не закрыт

Первая сессия ядра — передача Б1 «Трапезы». Источник истины сессии — Р-45…Р-51 «Трапезы», её Архитектура, Этап 8 Плана и разбор трёх диффов в её Журнале. Репозиторий `goigolden88/FamilyCore` заведён человеком пустым и склонирован в `Claude Projs\FamilyCore`. Сессия шла из пустой папки `Claude Projs\ЯдроСемейства`, а не из корня клона: история беседы привязана туда.

**Решения — все записаны в момент выбора**

- **Я-01 — нумерация «Я-NN»**; чужие «Р-NN» получают хозяина. Выбор человека из четырёх, рекомендация
- **Я-02 — корень репозитория = `src/shared` приложения**; `package.json` без `dependencies`, версии точные с `d86f0aa`; в `src/shared` приложения `npm install` не запускается
- **Я-03 — общий интерфейс получает `db` и `sync` React-контекстом**, код приложения — пропсами. Уточняет Р-47 «Трапезы»: одиночку `db` тянули ещё восемь общих файлов. Выбор человека из трёх, рекомендация
- **Я-04 — CI ядра**: typecheck и тесты на каждый пуш. Выбор человека, рекомендация
- **Я-05 — состав коммита-копии**: 57 файлов; сверх таблицы Р-48 «Трапезы» — `model.ts` и `vite-env.d.ts`, без `screenNames`
- **Я-06 — механика «Что нового» в ядро**, список `CHANGES` — приложению. Уточняет Р-48 «Трапезы»

**Сделано** — десять коммитов, по порядку передачи

1. Документы до кода: `CLAUDE.md`, `docs/01…06`, договор семьи в `02-Архитектура`
2. Копия `d86f0aa` — 57 файлов байт в байт через `git archive`, пути без `src/`
3. Хозяева ссылок: 163 «Р-NN» в 38 файлах — только комментарии
4. Инструменты: `package.json`, Vitest, `test.yml`
5. `core/`: `Base`, `Migration`, `Place`, `AppConfig` с `checkConfig`; `createDb`, `createLayout`, `createSync(config, db)`, `createImporting`; `FeedItem.kind` — строка
6. Общий интерфейс: `ui/core.tsx` (`CoreProvider`, `useCore`), вкладки `Layout` пропсом, `useWhatsNew(base, changes)`, `useFirstRun(own)`, `useFeed(feedItems)`, `ImportRecords` с пропсами; `notify.ts` → `createReminders(settings, { topics, idle })`; `sw.ts` → `startWorker({ remind })`
7. Каркас `styles.css`: 75 блоков из 178, первая нарезка
8. Сборка: `scripts/vite.ts` (`familyVite`), `scripts/icons.mjs` (`writeIcons`), `tsconfig.base.json`
9. Тесты на подставной «Полке» (`testing/shelf.ts`); новые — на `checkConfig`, две базы на одном origin, миграцию через `createLegacyBase`, README из конфига, `importing`, `createReminders`
10. Документы в конце: Архитектура с настоящим деревом и чеклистом «Как приложение подключает ядро», План, Журнал

**Находки по ходу**

- **Голый «Р-NN» в ядре — не всегда «Делу Время».** Механика синхронизации, слепка и слияния (`db`, `sync`, `github`, `layout`, `importing`, `Fold`, `Install`, `ScrollButtons`, `SyncSettings`) несёт номера **«Дневников»**: Р-07 в `db.ts` — «Слияние: последняя правка побеждает», а Р-07 «Делу Время» — «Приоритетов нет». Р-43 «Трапезы» и её `06-Процесс` («голый Р-NN — решение „Делу Время“») в этой части неверны. Хозяин каждой ссылки сверен по теме с `05` обоих приложений через `git show`
- **Р-47 «Трапезы» назвал четыре фабрики, а одиночку `db` импортировали ещё восемь общих файлов** — отсюда Я-03
- **`useWhatsNew` без `changes.ts` приложения не работает** — механика лежала в файле списка; отсюда Я-06
- **README данных и промпт различались не только именем** — тремя фразами о предмете; отсюда поле `about` сверх Р-47. Имя ядро не склоняет: тексты говорят «приложение «Трапеза»», а не «данные «Трапезы»»
- **У `importing.ts` на `d86f0aa` не было тестов** — теперь есть
- **Иконки после выноса те же:** фигуры «Делу Время» через `writeIcons` дают четыре PNG, байт в байт равные `public/` на `d86f0aa`

**Ошибки по ходу, мои**

- **Экранирование в скриптах правки.** Трижды: `node -e` в Bash и heredoc съели обратную косую черту (`\*`, `\d`, `\\`), дважды замены шли в неверном порядке — глобальная раньше частной. Поймано тем, что каждый скрипт падает на первом несовпадении до записи. Один раз скрипт был не атомарным: успел поправить `model.ts` и `notify.ts`, прежде чем упасть на третьем файле, — увидено в диффе, правки верные. Теперь: замены с косой чертой — только скриптом-файлом через инструмент записи, частные замены — раньше глобальных
- **Заготовил лишнее.** В `screens/changes.ts` добавил проверку списка изменений, которую никто не просил, — убрал до коммита: тесты списка есть у приложений

**Состояние: код Этапа 1 написан, этап не закрыт**

*Проверено машинно*

- `npm run typecheck` — код 0; `npm test` — код 0, 19 файлов, 325 тестов. На копии до выноса: typecheck — 12 ошибок, все про файлы «Делу Время»; тесты — 275 в 16 файлах из 17
- Коммит-копия: отпечатки 57 blob совпали с `d86f0aa`
- Коммит ссылок: дифф без вставленных хозяев совпадает построчно — код не тронут
- Каркас стилей: дифф — только удаления и шапка файла
- Иконки: 4 PNG байт в байт с `d86f0aa`
- Поиск по ядру: `deluvremya` — нет; импортов файлов приложения — нет; `indexedDB` — только в `core/db.ts` и тестах; голых «Р-NN» — нет, кроме цитат в тексте Я-01
- Чистый клон с GitHub: `npm ci`, `npm run typecheck` — 0, `npm test` — 0, 325 тестов
- CI «Проверка ядра» на `de8021c` — success

*Проверено на устройстве* — нечего: у ядра нет сайта

*Не проверено* — всё записано в План, «Хвосты закрытых этапов»: сборка работника из двух файлов; швы стилей и `notify` на живом приложении; типы `SharedDb` и `Sync` против настоящей «Трапезы»

**Не описано в документах — на обсуждение**

- **Р-43 «Трапезы» и её `06-Процесс` в части «голый Р-NN — это „Делу Время“» неверны** (находка выше). Правится в «Трапезе» её сессией; после перевода вопрос уйдёт вместе с копией ядра
- **`.stats`, `.link-btn`, `.icon-btn`, `.chips`** — первая нарезка оставила их приложениям, хотя у «Делу Время» и «Трапезы» они, вероятно, одинаковы. В Плане, «Отложено»
- **Шапка экрана с шестерёнкой и точкой** взята в каркас сверх слов Р-48 «Трапезы»: точку считает `syncDot` ядра, шапка одинакова у трёх приложений
- **Пустая папка `Claude Projs\ЯдроСемейства`** — из неё запускалась эта сессия; человек решает, удалить ли её

**Следующий шаг**

**Передача Б2 «Трапезы»**: перевод «Трапезы» на ядро сессией из её корня, по чеклисту `02-Архитектура`, «Как приложение подключает ядро». Если ядру чего-то не хватит — сессия отсюда, решение Я-NN.
