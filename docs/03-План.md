# План

Обновлён: 25.09.2026

Здесь только то, что предстоит. Что уже сделано — в Журнале.

## Логика порядка

Ядро рождается из копии, а не пишется заново: первая версия — файлы `d86f0aa` «Делу Время» байт в байт, дальше — вынос конфига (Р-49 «Трапезы»). Так каждая строка ядра прослеживается до кода, который год работал на телефоне, а дифф выноса показывает ровно то, что стало параметром.

Проверка шва — два шага, и ни один не заменяет другой: ядро собирается и проходит тесты **без приложения рядом** — сделано 18.09.2026, Этап 1; приложение переезжает на ядро **без изменения данных** — сессией из его корня, первой — «Трапеза».

---

## Хвосты закрытых этапов

Этап 1 «Ядро собирается без приложения» — код написан 18.09.2026: зелёный локально, в чистом клоне и в CI. Остаётся то, что здесь не проверить:

- **Работник собирается из двух файлов** — `src/sw.ts` приложения импортирует `startWorker` ядра, а `self.__WB_MANIFEST` стоит в файле ядра. `injectManifest` вписывает манифест в собранный работник целиком, и так быть обязано, но проверит это только `npm run build` «Трапезы» и работа без сети
- **Два шва, найденные глазами** — граница каркаса `styles.css` и разрез `notify` (открытые вопросы `05`): прогон «Трапезы» и вёрстка на телефоне
- **Типы общего интерфейса на настоящем приложении** — `tsc` «Трапезы» поймал: `db` не подходил под `SharedDb`. В ядре починено 18.09.2026 (`d1f282f`), проверка на «Полке» — `ui/core.test.ts`. Остаётся подтверждение: `tsc` «Трапезы» зелёный без приведений после обновления указателя

Если что-то из этого не сойдётся — правка ядра сессией отсюда, решение Я-NN, а не правка в `src/shared` «Трапезы» на месте.

## Этап 2 — «Трапеза» на ядре

Не здесь: сессия из корня «Трапезы» по её передаче Б2 (Р-49 «Трапезы»). Чеклист перевода — `02-Архитектура`, «Как приложение подключает ядро».

**Готово когда** — по Р-49 «Трапезы»: её `npm test`, `build`, `smoke` зелёные по коду выхода; установленное приложение на телефоне открывает прежнюю базу без переустановки.

Этап 3 «Договор итогов для метаприложения» закрыт 25.09.2026 — Журнал.

## Телеграм-бот «Тотального Учёта» — рамка есть (Я-33, Я-34)

Этап 4 «Тотального Учёта» — его сессиями: где живёт, расписание, что в сообщении, как говорит об ошибке — его Р-NN. Здесь — только если выйдет за рамку: суммы, новые названия, чат не личный, чтение записей вместо среза. Промпт — Журнал, 25.09.2026, «Что нести в `TotalUchet`».

## Указатель — не старше `553ed14` (Я-28, Я-29)

Не договор, не срочно: у приложения, которое не обновит указатель, ничего не ломается. «Дневники», «Злотые», «Тотальный Учёт» — на `cb72d16`, сделано. **«Делу Время» (`6a5da3f`) и «Трапеза» (`ee1dc37`)** — при следующем заходе; видимое — строкой в `changes.ts`: «Проверить доступ» больше не говорит «запись разрешена» (Я-28). Из Я-30…Я-34 им в коде ничего не нужно: их `link` — путь с датой (Я-31), их подписи уходят боту (Я-33) — сказать строкой в своём `02`, «Срез итогов». Промпты — Журнал, 25.09.2026.

## Тексты приложений под Я-25 и Я-27

«Делу Время» — `Help.tsx` («на каждом устройстве свой токен»), `02` стр. ~251; видимое — строкой в `changes.ts`, при следующем заходе. «Дневники» (`4914e9d`), «Злотые» (`2f11258`), «Трапеза» (`8fc2468`), «Тотальный Учёт» — сделано.

## Дальше

- **Подключение «в пару кликов»** — отдельной сессией ядра по открытым вопросам `05`: токен раз на устройство, потом ссылка или QR, страница для друга (25.09.2026)
- **«Дача» — новое приложение, сразу на ядре** (Я-22): при старте — сессия ядра вписывает её `dbName` в договор; первая строка кода — после её собственных документов
- **Семья на ядре целиком** — «Трапеза», «Злотые», «Делу Время», «Дневники» (Журнал, 24.09.2026); нехватку ядра, если найдут, приносят сюда сессией сопровождения

## Отложено

| Что | Почему не сейчас | Когда вернуться |
|---|---|---|
| Отозвать прежние токены по приложению (`06-Процесс`, «Токены семьи», шаг 7) | работе не мешают: «семья» вписана во все приложения, прежние живут до своего срока (человек, 25.09.2026) | когда будет минута; до их срока — лучше раньше: каждый лишний токен с записью — лишняя утечка |
| Каркас прогона `smoke.mjs` в ядро | шов между помощниками CDP и сценариями ищется руками (Р-48 «Трапезы») | если правка помощников в четырёх копиях надоест |
| Общий `deploy.yml` вызываемым workflow | подмодулем не раздаётся (Р-48 «Трапезы») | если разъедется |
| `through` у строки (`format: 2`) и общий код «не загружено» | нужны одному приложению — «Злотым»; обход — основанием и своим `not-loaded` (Я-32) | когда разная свежесть строк отрезка или «ни одного дня не загружено» понадобятся второму — «Даче», например |
| Своё название приложения настройкой — параметром ядра | просит одно приложение: имя у «Тотального Учёта» (Р-03 «Тотального Учёта»); вкладки «Делу Время» (Р-26 «Делу Время») — другое; манифест за настройкой всё равно не следует | если попросит второе приложение |
| `.stats`, `.link-btn`, `.icon-btn`, `.chips` — в каркас | в таблице Р-48 «Трапезы» их нет; первая нарезка оставила их приложениям | если при переводе окажутся одинаковыми у двух приложений |
