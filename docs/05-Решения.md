# Решения

Какие были развилки, что выбрали и почему. Отвечает на вопрос «почему так, а не иначе».
Записывается в момент выбора, чтобы через месяц не переобсуждать заново.
Старые записи не переписываются: отмена — новая запись со ссылкой «отменяет Я-NN».

**Решения, из которых ядро родилось, — не здесь.** Р-45…Р-51 «Трапезы» (18.09.2026): вынос начинается сейчас, подмодуль, шов конфигом, состав, эталон и порядок перевода, имя репозитория, договор семьи. Их история — Р-06, Р-16, Р-22, Р-43 «Трапезы». Ядро на них ссылается и их выполняет; пересмотр — решением здесь со ссылкой.

---

### Я-01 · Решения ядра нумеруются «Я-NN»; чужие «Р-NN» в коде получают хозяина
18.09.2026

Повод: открытый вопрос «Трапезы» — «как `FamilyCore` нумерует свои решения» (Р-43, Р-49 «Трапезы», «Цена» п. 2). Первое решение ядра.

**Как было.** В коде, скопированном из «Делу Время», около 170 ссылок «Р-NN»: `core/dates.ts` говорит «неделя — с понедельника (Р-41)» — это Р-41 «Делу Время». «Трапеза» держала правило «голый Р-NN в файлах ядра — чужой» (её Р-43), но номера уже пересеклись: у неё свой Р-41 появился 18.09.2026. В общем репозитории у этих номеров хозяина нет вовсе.

**Решено.**

- Решения ядра — **«Я-NN»**: Я-01, Я-02… — в `docs/05-Решения.md` этого репозитория
- Решение приложения — **«Р-NN» с хозяином**: «Р-41 „Делу Время“», «Р-47 „Трапезы“». Голого «Р-NN» в ядре нет
- Унаследованные ссылки получают хозяина **отдельным коммитом, только комментарии**, сразу после коммита-копии и до выноса. Хозяин проверяется по `05` «Делу Время» на `d86f0aa`, а не на глаз: часть ссылок там — решения «Дневников»
- Правило, которое ядро меняет, получает своё Я-NN со ссылкой на прежнее; правило, которое ядро только несёт, остаётся со ссылкой на родное решение

Выбор человека из четырёх, рекомендация.

**Почему.** Ядро лежит внутри каждого приложения, в `src/shared`. В одном дереве будут и «Р-41» приложения, и ссылки ядра, и отличить их обязан сам текст ссылки, а не путь к файлу. Префикс кириллицей читается в русских комментариях как слово: «Я» — ядро.

Отвергнуто:
- **«FC-NN»** — то же самое, но латиница посреди русского текста;
- **свои «Р-NN» с Р-01** — единообразно с семьёй, но в одном дереве два разных Р-41, то есть ровно та беда, от которой уходим;
- **без ссылок в коде** — «почему» уходит только в `docs/`, связь строки с решением теряется, а 170 ссылок вычищаются впустую.

**Цена.** Коммит комментариев в ~50 файлах — дифф к `d86f0aa` растёт не по делу. Зато он отдельный и пуст по коду: в диффе выноса комментарии не мешают. Ссылки вида «Р-NN „Делу Время“» длиннее голых.

---

### Я-02 · Корень репозитория — точка монтирования `src/shared`; `package.json` ядра — только для тестов и типов
18.09.2026

Повод: Р-47 «Трапезы» ставит подмодуль в `src/shared/` с `core/` и `ui/` внутри. Отсюда форма самого репозитория.

**Решено.**

- Код лежит **в корне** репозитория: `core/`, `ui/`, `screens/`, `sw.ts`, `styles.css`, `scripts/`. Рядом — `docs/`, `CLAUDE.md`, `package.json`, `tsconfig.json`, настройки тестов. Всё это приезжает в `src/shared` приложения
- **`package.json` без `dependencies`.** Только `devDependencies` для тестов и типов: `typescript`, `vitest`, `fake-indexeddb`, типы React, `react`, `react-router-dom`, `workbox-*`, `vite` и `vite-plugin-pwa` для типов фабрики и работника
- **Версии — ровно те, что в `package-lock.json` «Делу Время» на `d86f0aa`**, записанные точными, без `^`: иначе типы в ядре и в приложении разойдутся
- **`npm install` внутри `src/shared` приложения не запускается никогда.** Иначе импорт `react` из `src/shared/ui` найдёт `src/shared/node_modules/react` раньше приложения, и на странице окажутся две копии React

**Почему.** Иначе путь внутри подмодуля (`src/shared/src/core/…`) расходился бы с деревом, записанным в Архитектуре «Трапезы», а подмодуль нельзя подключить подпапкой.

Отвергнуто: **код в `src/` репозитория** — `src/shared/src/core` в приложении; **отдельный репозиторий под документы** — второй источник истины.

**Цена.**

1. `docs/`, `CLAUDE.md`, тесты ядра видны внутри приложения. Тесты ядра увидят `tsc` и `vitest` приложения: включать их там или исключить — решается при переводе «Трапезы» (передача Б2).
2. Правило про `npm install` держится памятью; нарушение ловится прогоном приложения (две копии React ломают хуки сразу).

---

### Я-03 · Общий интерфейс получает `db` и `sync` React-контекстом; код приложения — пропсами
18.09.2026

Повод: разбор `d86f0aa`. Р-47 «Трапезы» делает фабриками четыре файла `core/`, но одиночку `db` импортируют ещё восемь общих файлов: `useFold`, `report`, `useFirstRun`, `useWhatsNew`, `useFeed`, `ImportRecords`, `notify` и `sync`; к `sync` так же привязаны `useSync`, `syncDot`, `SyncSettings`, `backup`. Когда `db` создаёт приложение, им негде его взять. Уточняет Р-47 «Трапезы».

**Решено.**

- **Хуки и компоненты ядра** берут `db`, `sync` и `config` из React-контекста: приложение один раз оборачивает дерево в `<CoreProvider value={…}>`, внутри — `useCore()`. Вызовы `useFold(id)`, `<Fold>` и прочих у приложений не меняются
- **Чистые модули** (`ui/report.ts`, `ui/backup.ts`, механика напоминаний, работник) получают `db` и что им нужно **аргументом** или собираются фабрикой
- **Код приложения** — вкладки `Layout`, список `changes` для «Что нового», строки ленты и разделы импорта из `registry.ts`, хранилища первого запуска, правило напоминания — приходит **пропсами и аргументами**, а не импортом

Выбор человека из трёх, рекомендация.

**Почему.** Контекст — обычный путь React передать объект, созданный снаружи: зависимость видна в коде (провайдер в `app.tsx`), а цена переезда у приложений минимальна — меняется один файл, а не каждый вызов `useFold`.

Отвергнуто:
- **фабрики на всё** (`createUi(core)` возвращает готовые хуки) — полностью явно, но ещё один файл-посредник и правка каждого импорта `Fold`/`useFold` в четырёх приложениях;
- **регистрация `bindCore(core)`** — `app/core.ts` кладёт `db` в модуль ядра, дальше всё как было. Дешевле всего при переезде, но это невидимая зависимость, ломающаяся порядком импортов, — ровно то, за что Р-47 «Трапезы» отверг алиас.

**Цена.**

1. Хук ядра вне `CoreProvider` не работает; `useCore()` падает с внятным текстом, а не молча.
2. Работник и `notify` живут вне React — у них свой путь, аргументом; двух способов получить `db` в ядре два, и это надо помнить.
3. У `app.tsx` каждого приложения появляется провайдер.

---

### Я-04 · Ядро проверяет себя само: `npm test` и `tsc` на каждый пуш
18.09.2026

Повод: ядро стало эталоном для четырёх приложений (Р-49 «Трапезы»), а своей сборки и сайта у него нет — красное ядро иначе обнаружится только при обновлении указателя в приложении.

**Решено.** `.github/workflows/test.yml`: `npm ci`, `npm run typecheck`, `npm test` на пуш и пул-реквест. Итог — по коду выхода. Выбор человека, рекомендация.

**Почему.** Проверка стоит минуту и ловит поломку до того, как указатель подмодуля уехал в приложение. В `src/shared` приложения папка `.github` ни на что не влияет: GitHub читает workflow только из корня своего репозитория.

Отвергнуто: **только локальная проверка перед пушем** — так живут приложения, но у приложения поломку видно сразу на сайте, у ядра — нет.

**Цена.** Ещё один workflow на обслуживании; зелёный CI ядра не значит, что приложения собираются: это проверяет только их собственный CI после обновления указателя.

---

### Я-05 · Коммит-копия — 57 файлов `d86f0aa`: сверх таблицы Р-48 «Трапезы» взяты `model.ts` и `vite-env.d.ts`, без `screenNames`
18.09.2026

Повод: сверка таблицы Р-48 «Трапезы» с деревом `d86f0aa`.

**Решено.** Первым коммитом кода — `git archive d86f0aa` этих файлов, байт в байт, пути без `src/`:

- `core/` целиком — 17 файлов, **включая `model.ts`**
- `ui/` — 25 файлов: всё из таблицы Р-48 «Трапезы» с тестами; **без** `screenNames`, `screenNames.test`, `useScreenNames` — свои названия экранов «Делу Время» (их Р-26), Р-16 «Трапезы» в силе
- `screens/` — 7: `WhatsNew`, `useWhatsNew`, `useFirstRun`, `firstRun` с тестом, `ImportRecords`, `useFeed`
- `sw.ts`, `notify.ts` с тестом, `styles.css`, **`vite-env.d.ts`**, `scripts/icons.mjs`, `vite.config.ts`, `tsconfig.json`

**Почему.**

- **`model.ts`** — файл приложения (Р-47 «Трапезы»), но `Base` и `Migration` лежат в нём. Взятый целиком и урезанный следующим коммитом, он показывает в диффе, откуда пришёл договор
- **`vite-env.d.ts`** в таблице Р-48 «Трапезы» нет, но без него ядро не собирается: он объявляет `__BUILD_TIME__` для отчёта об ошибке и `periodicSync` для напоминаний. Это часть каркаса сборки
- **`vite.config.ts`, `tsconfig.json`** — берутся как есть, чтобы фабрика конфига и `tsconfig` для `extends` были диффом к ним, а не новыми файлами

Коммит-копия не собирается — `notify`, `ImportRecords`, `useFeed`, `Layout`, `WhatsNew` тянут файлы приложения, — и так и задумано: дифф следующих коммитов и есть весь вынос.

**Цена.** Выносом добавляются строки, которых не было в `d86f0aa`, и удаляется половина `model.ts` — дифф тяжелее, чем при «чистом» старте. Взамен каждая строка ядра прослеживается до `d86f0aa`.

---

### Я-06 · Механика «Что нового» — в ядро; список `CHANGES` остаётся приложению
18.09.2026

Повод: вынос `useWhatsNew`. Р-48 «Трапезы» относит `WhatsNew` и `useWhatsNew` к ядру, а `changes.ts` — к приложению. Но в `d86f0aa` в `changes.ts` лежат не только записи, а и тип `Change` с функциями `unseenChanges` и `latestChange`: без них хук ядра не работает. Уточняет Р-48 «Трапезы».

**Решено.** Тип `Change`, `unseenChanges`, `latestChange` и их тесты переезжают в `screens/changes.ts` ядра. В `src/changes.ts` приложения остаётся список `CHANGES` и тест самого списка (id подряд, даты не убывают, пустых строк нет). `useWhatsNew(base, changes)` получает список аргументом (Я-03).

**Почему.** Механика одинакова у всей семьи — взята из «Дневников» и не менялась ни в «Делу Время», ни в «Трапезе». Список — единственное, что у приложений разное, и он пишется руками в том же коммите, что видимое изменение.

Отвергнуто: **передавать функции вместе со списком** — три аргумента хука вместо одного ради того, чтобы механика осталась в четырёх копиях.

**Цена.** `changes.ts` приложения при переводе теряет две функции и импортирует `Change` из `shared/screens/changes.ts`.

---

### Я-07 · У `ImportPlan` — заметки, правки и удаления; сводка импорта — отдельным компонентом `ImportSummary`
23.09.2026

Повод: находки «Злотых» (их `06-Процесс`, «Шаблоны»; Журнал «Злотых», 20–23.09.2026), сверенные через `git show` на `4b303ed`. Меняет правило, которое ядро несло от Р-60 «Дневников»: «импорт только добавляет».

**Как было.** У разбора раздела два исхода: запись попадёт в базу (`writes`, посчитана в `added`) или нет (`issues`). У «Злотых» импорт делает больше, и сказать об этом ему нечем:

1. **Сообщение — отказом.** «Округление» (учёт и капитал) и «итог периода заменён операциями» лежат в `issues`: сводка пишет «не попадёт в базу: N», а записи попадают. Повод в просьбе назван «склейкой переводов» — её убрал Р-19 «Злотых», но беда та же
2. **Правка — молча.** Сошедшаяся сверка ставит счёту `loadedThrough` (Р-26 «Злотых»): запись идёт в `writes`, но ни в `added`, ни в `issues` её нет
3. **Удаление — отказом.** Итог периода, который заменили операции (Р-02 «Злотых»), уходит надгробием через `writes`, а называется в `issues`. Кнопка «Загрузить N» считает надгробие вместе с добавленным
4. **Общий абзац лжёт.** `ImportRecords` обещает «импорт только добавляет: то, что уже есть, не перезаписывается» — у «Злотых» это неправда дважды
5. **Готовый текст негде показать.** «Подтянуть курсы» получает от источника тот же файл импорта, но сводки ядра для него нет — у кнопки своя, вторая, поверх той же `planImport`

**Договор семьи не затронут.** `ImportPlan` живёт между разбором приложения и экраном ядра, на миг сводки: в базе, в репозитории данных и в том, что прочтёт метаприложение, не меняется ничего. Это правка API ядра, а не договора (Р-51 «Трапезы»): релиза на все приложения сразу она не требует.

**Решено.**

- У `ImportPlan` три поля, **необязательных**:
  - `notes?: Note[]` — запись загрузится, но человеку стоит посмотреть: округление, подозрение, замена. `Note = { section, title, text }`
  - `changed?: Count[]` — сколько уже имеющихся записей изменится
  - `removed?: Count[]` — сколько уйдёт надгробием
- `Count = { count, forms }` — прежний `Added`; `Added` остаётся его синонимом. `mergeResults` сводит новые поля так же, как `added` и `issues`, и отдаёт их всегда
- **Сводка** по порядку: «Добавится» → «Изменится» → «Удалится» → «Уже есть — пропущено» → **«Загрузится, но стоит посмотреть: N»** (не цветом ошибки) → «Не разобрано — в базу не попадёт: N». Счёт отказов — только `issues`
- **Общий абзац** — правда для любого приложения: повторная загрузка ничего не удвоит; если меняется что-то уже имеющееся — сводка назовёт это до записи
- **`ImportSummary({ text, planImport, onApplied, onCancel })`** — сводка и запись, вынесенные из `ImportRecords`: разбирает готовый текст, показывает сводку, пишет по кнопке в порядке `stores` конфига. `ImportRecords` пользуется ею сам; приложение ставит её там, где текст у него появился
- Что правка или удаление, говорит **приложение**, как и `added`: ядро не сравнивает `writes` со слепком

Выбор человека, рекомендация.

**Почему.** Сводка — единственное место, где человек видит, что импорт сделает с его данными, до записи. Отказ, заметка, правка и удаление — четыре разных ответа на «что будет», и слово у каждого своё. Необязательные поля — чтобы приложение без заметок и правок («Трапеза») обновило указатель без единой правки своих разборов.

Отвергнуто:
- **поля обязательными** — строже, но каждый раздел каждого приложения не соберётся, пока в нём не появятся пустые списки;
- **правку и удаление одним `changed`** — «Изменится: 1 итог» прячет, что итог исчезнет; удаление человеку важнее увидеть;
- **считать правки и удаления ядром**, сравнивая `writes` со слепком по `id`, — у ядра нет слов для чужих записей, и у одного числа стало бы два источника;
- **пропс `initialText` у `ImportRecords`** — тянет туда, где появился текст, весь экран импорта с полем, файлом и промптом, либо переход на экран импорта с текстом в состоянии роутера.

**Цена.**

1. Честность сводки держится на приложении: забыло назвать правку — сводка промолчит, как раньше. Ловится тестами разбора приложения.
2. Общий абзац меняется у всех — у «Трапезы» тоже: видимое изменение, строка «Что нового» при обновлении указателя по её выбору.
3. `Count` в `changed` и `removed` — склонение существительного, а не действия: «Изменится: 2 счёта», «Удалится: 1 итог периода».

---

### Я-08 · Выборка по индексу `db.getByIndex`; индексы хранилищ версии 1 заморожены вместе с `v1Stores`
23.09.2026

Повод: находка Этапа 0 «Злотых» (их Журнал, 20.09.2026) — ядро заводит индексы из `config.indexes`, а читать по ним не даёт: `getAll` читает хранилище целиком. Отложено человеком до конца «Злотых».

**Решено.**

- `db.getByIndex(store, index, match, { includeDeleted? })` — записи хранилища, у которых поле индекса совпало. `match` — ключ (`string | number`) или диапазон `{ from?, to? }`, границы включительно. `IDBKeyRange` не выходит из `core/db.ts`. Надгробия отсеиваются, как у `getAll`
- Индекс проверяется **по базе на устройстве** (`indexNames`), а не по конфигу. Нет его — внятная ошибка с именами хранилища и индекса
- Записи без поля (`null`, не задано) в индекс не попадают — так устроен IndexedDB; сказано в комментарии
- **`indexes` хранилищ версии 1 заморожен вместе с `v1Stores`.** `createStores` заводит их только на свежей базе: индекс, дописанный в конфиг позже, появится у новой установки и не появится у давней, а миграция, заводящая его же, на свежей базе упадёт — индекс уже есть. Индекс после первого релиза — только шагом миграции (`additive: true`). Правило — в комментарии `AppConfig.indexes` и в `02-Архитектура`

Выбор человека, рекомендация.

**Почему.** Индекс, по которому никто не читает, — обещание без применения (Р-32 «Злотых»). История за годы — тысячи записей, и месяц не должен читать их все. Проверка по устройству, а не по конфигу, — потому что конфиг и база могут разойтись ровно так, как описано выше.

Отвергнуто:
- **`IDBKeyRange` наружу** — к IndexedDB ходило бы не только `core/db.ts`;
- **сверять индекс с `config.indexes`** — индексы хранилищ, заведённых миграцией, в конфиге только для чтения глазами, а на устройстве они есть;
- **переименовать поле `indexes`** в честное «не читается» — вместо того чтобы дать читать.

**Цена.** Составных ключей и сортировки по убыванию нет — не понадобились. Правило заморозки держится памятью: `checkConfig` не видит, что было в первом релизе.

---

### Я-09 · Годовая нарезка в раскладке: `Place` с `split: 'year'`; нарезка места после первого релиза не меняется
24.09.2026

Повод: находка перевода «Делу Время» (Журнал ядра, 24.09.2026), сверенная через `git show a913dcb` «Дневников». Их `cycleEvents` и `content` лежат по годам — `cycles/ГГГГ.json`, `content/ГГГГ.json`, без даты — `undated.json` (Р-08, Р-33, Р-34 «Дневников»). `Place` ядра и договор знали только «одним файлом» и «по месяцам». Переложить их данные по месяцам — миграция репозитория данных, а перевод данных не трогает (Р-49 «Трапезы»): без годовой нарезки «Дневники» не перевести.

**Это правка договора семьи** — раскладку читает метаприложение (Р-51 «Трапезы»). Решение здесь и релиз на все приложения: указатель обновляют все, хотя коду приложений без годовых мест править нечего — `Place` только расширяется.

**Решено.**

- У `Place` третий вид: `{ split: 'year'; dir; dateOf }`. Файл — `<dir>/ГГГГ.json`, год — по дате записи любой точности: день `ГГГГ-ММ-ДД` и месяц `ГГГГ-ММ` (Р-25 «Дневников») дают один год. Без даты и с нечитаемой датой — `<dir>/undated.json`, как у месячной нарезки
- Год в имени файла — **первые четыре знака даты**, а не число: `0999` остаётся `0999.json`, и `storeOf` его узнаёт. У «Дневников» на `a913dcb` год — число из `yearOf`: для лет 1000…9999 файл тот же побайтно, год до 1000 у них дал бы файл, который их же `storeOf` не узнаёт
- `storeOf` годовой папки узнаёт только `ГГГГ.json` и `undated.json`, месячной — только `ГГГГ-ММ.json` и `undated.json`: чужой нарезки файл в папке — не наш и не трогается
- README данных: строка `<dir>/ГГГГ.json`; фраза о непереписываемом прошлом называет годы, если у приложения есть годовые места. У приложения без них README не меняется ни на байт — это закреплено тестом
- **Нарезка места после первого релиза не меняется** — ни вид (`none` / `month` / `year`), ни `dir`, ни путь. Прежние файлы после смены стали бы для `storeOf` чужими: на сервере осталась бы копия, а записи из них не дошли бы до свежего устройства. Смена нарезки — миграция репозитория данных, а не правка конфига. Правило — в комментарии `Place` и в договоре

Выбор человека, рекомендация.

**Почему.** Годовая нарезка — не частность «Дневников»: она нужна хранилищу, в которое пишут редко (отметки цикла, просмотренное), — там месячный файл дробил бы историю на десятки файлов по одной записи. Р-28 «Делу Время» выбрал месяцы для хранилища, куда пишут на каждое нажатие; оба выбора верны для своих хранилищ, и выбирает их приложение полем `places`.

Отвергнуто:
- **переложить данные «Дневников» по месяцам** — миграция данных в переводе, который обязан их не трогать;
- **`split` параметром-функцией** («имя файла по записи») — договор перестал бы быть перечислимым: метаприложение и `storeOf` не знали бы, какие файлы наши;
- **год числом, как у «Дневников»**, — повтор ошибки на годах до 1000 ради побайтного совпадения там, где его и так нет.

**Цена.**

1. Релиз на все приложения без видимого изменения у трёх из них: указатель обновляется ради договора.
2. Правило «нарезка не меняется» держится памятью: `checkConfig` не видит, какой нарезка была в первом релизе.
3. Метаприложение, когда появится, читает три вида файлов, а не два.

---

### Я-10 · Метаприложение планируется: отсрочка Р-51 «Трапезы» снята для договора, не для аналитики
24.09.2026

Повод: семья на ядре целиком (Журнал, 24.09.2026), человек начинает планирование метаприложения. Первый материал — записка сессии «Дневников» о том, что метаприложению можно взять у них (сверена ими с `fba632a`).

**Как было.** Р-51 «Трапезы»: контуры метаприложения не пишутся нигде, пока семья не собрана и не накоплены данные. `01-Ядро`: «Не делаем: метаприложение и его контуры».

**Решено.**

- Из двух условий Р-51 «Трапезы» выполнено одно: семья собрана, данных — нет. Планируется то, что от накопленных данных не зависит: какой срез «итогов» отдаёт каждое приложение, как метаприложение его читает, доступ к чувствительным данным, «что требует внимания», общая хроника
- **Не планируются** связи между приложениями («ел не то — заболел») и ИИ-модуль: какие вопросы настоящие, покажут данные. Здесь Р-51 «Трапезы» в силе
- **Здесь — только договорная часть**: итоги, путь чтения, доступ; решениями Я-NN, релизом на все приложения. Экраны и вопросы метаприложения — его собственный репозиторий. Ядро про метаприложение не знает так же, как про любое приложение
- **Разведка — отсюда**, через `git show HEAD` четырёх приложений, одной анкетой на всех: восемь пунктов записки «Дневников». Сводка каждого приложения уходит на вычитку его сессии: из кода не видно решений, которые в коде не записаны
- **Порядок:** разведка → Я-11 «кто считает итоги» → решение о том, где лежит срез и в какой форме, — первый потребитель формы — недельный обзор «Делу Время» (Р-79 «Дневников») → репозиторий метаприложения

Выбор человека, рекомендация.

**Почему.** Договор итогов нужен уже сейчас и не одному метаприложению: недельный обзор «Делу Время» ждёт фактов недели из «Дневников» (Р-79 «Дневников») — это та же задача. «Что требует внимания» уже лежит в форме ядра (`Topic`, `Notice` в `notify.ts`). Ни то, ни другое не требует месяцев данных; связи между приложениями — требуют.

Отвергнуто:
- **ждать дальше** — второй потребитель итогов уже есть, и без договора он решит задачу у себя по-своему;
- **сразу репозиторий метаприложения** — пустое место, пока не решено, что и как оно читает; довод Р-51 «Трапезы» здесь в силе;
- **контуры метаприложения в `docs/` ядра** — ядро не знает про приложение.

**Цена.**

1. Разведка из кода видит меньше, чем сессия приложения: вычитка — ещё по одному проходу в четырёх корнях.
2. Итоги — правка договора: каждый шаг — релиз на все приложения.

---

### Я-11 · Итоги считает приложение — хозяин данных; метаприложение читает готовый срез
24.09.2026

Повод: развилка Я-10 — кто считает итоги.

**Решено.**

- Приложение считает итоги **своими чистыми функциями** и отдаёт машинный срез в форме договора. Метаприложение сырых записей приложений не читает и их моделей не знает
- **Не решено:** где срез лежит, как метаприложение его читает и какой он формы. Это следующее решение — после разведки (Я-10)

Выбор человека из трёх, рекомендация.

**Почему.** Правила расчёта живут у хозяина данных и уже покрыты тестами: число — вместе с основанием (Р-38, Р-44 «Дневников»), догадка не выдаётся за расчёт (Р-27 «Дневников»), «закончено» — по статусу (Р-42 «Дневников»). Повтор в метаприложении — вторая копия, которая расходится молча: ровно та беда, из которой родилось ядро. Договор остаётся узким — форма среза, а не типы записей четырёх приложений, — и модель приложения по-прежнему меняется его миграцией, без релиза на всех.

Отвергнуто:
- **метаприложение читает сырые записи из репозиториев данных и считает само** — договор вбирает модели всех приложений, каждая миграция приложения становится правкой договора, правила расчёта повторяются;
- **IndexedDB соседей напрямую на общем origin** — одно устройство, чтение в обход договора, и открытое соединение мешает миграции соседа: открывать без номера версии, закрывать по `versionchange` (записка «Дневников», п. 8; открытый вопрос Р-51 «Трапезы»).

**Цена.**

1. Срез пишет и поддерживает каждое приложение — работа в четырёх репозиториях.
2. Срез свеж настолько, насколько свежа последняя синхронизация приложения.
3. Вопрос, на который срез не отвечает, — правка договора, а не только метаприложения.

---

### Я-12 · Первый потребитель среза — метаприложение; обзор «Делу Время» даёт на него ссылку. Уточняет порядок Я-10
24.09.2026

Повод: вычитка «Делу Время» (`07-Разведка`, раздел «Вычитка»). Я-10 назвал первым потребителем формы недельный обзор «Делу Время» (Р-79 «Дневников»). План «Делу Время», «Отложено», для пути через метаприложение записал иначе: факты соседей появятся в метаприложении «ссылкой или своим экраном, а не блоком обзора», приложения друг о друге не знают; человек 14.09.2026 — интеграции «метаприложением».

**Противоречие.** Если обзор читает срезы соседей сам, ему нужны токены к чужим репозиториям — путь, который в их Плане записан с ценой «два секрета, две точки отзыва». Если не читает — форму проектировали бы под потребителя, который её не прочтёт.

**Решено.**

- **Факты недели соседей показывает метаприложение.** Обзор недели «Делу Время» даёт на него ссылку, срезов соседей не читает. Приложения семьи друг о друге не знают по-прежнему
- **Первый потребитель формы среза — метаприложение**, его недельная сводка. Порядок Я-10 уточняется: разведка → решение о месте, пути и форме среза под метаприложение → репозиторий метаприложения
- **Зерно, которое метаприложение просит у всех, — неделя с понедельника по воскресенье (Р-41 «Делу Время») и календарный месяц.** Отрезок в срезе называется явными `from` и `to` (вычитка «Злотых»): у приложения, чей «месяц» не календарный, это видно, а не подразумевается

Рекомендация сессии «Делу Время», выбор человека.

**Почему.** Так держится граница семьи: приложение не знает соседей, и токен к чужому репозиторию живёт в одном месте — у читателя всех. Недельный обзор «Делу Время» при этом получает свои факты — там, где они сведены со всех приложений, а не только с «Дневников».

Отвергнуто:
- **обзор «Делу Время» читает срезы соседей сам** — токены к чужим репозиториям в приложении, которое про них не знает; второй читатель срезов рядом с метаприложением;
- **оставить Я-10 как есть** — форма под потребителя, который её не прочтёт.

**Цена.**

1. Этап 3 не закрыть без метаприложения: «хотя бы один настоящий потребитель читает» (План) — теперь это его код.
2. Факты соседей в обзоре — лишний тап: ссылка, а не блок.

---

### Я-13 · Деньги в срезе — по потребителю, тремя ступенями; имена людей — никогда
24.09.2026

Повод: вопрос разведки к «Злотым» — можно ли отдавать деньги в срез, который читает кто-то кроме приложения. Ответ их сессии (`07-Разведка`, «Вычитка», «Злотые»).

**Решено.** Деньги уходят в срез по названному потребителю, а не впрок:

1. **Всегда, без сумм:** есть ли записи за месяц; по какое число доведены выписки (`recordedThrough`, а не «есть хоть одна запись»); сколько регулярных ждёт; дата последнего снимка капитала. Этого хватает для «требует внимания» и хроники
2. **По просьбе потребителя:** норма сбережений — долей или видом «в N раз», всегда с основанием и причиной «не посчитано». Голая доля нарушает Р-07 «Злотых»
3. **Суммы — только названному потребителю и в названном составе:** в базовой валюте с кодом, основанием и списком не вошедшего без курса. Капитал и остатки по счетам — не сейчас: их не просит ни один потребитель. Долги — только итогом в базовой валюте

**Имена людей из долгов в срез не идут никогда и ни в каком виде** (Р-01 «Злотых»): это третьи люди, которые ни на что не соглашались.

Позиция сессии «Злотых», выбор человека, рекомендация.

**Почему.** Метаприложение собирает в одном месте токены ко всем репозиториям — риск в читателе, а не в файле среза: в репозитории данных «Злотых» срез не раскрывает сверх того, что там уже лежит (Р-03 «Злотых», «Цена», п. 3). Значит, в срез идёт не всё, что можно, а то, что просит названный потребитель. Норма сбережений менее чувствительна, чем суммы, и отвечает на главный вопрос «Злотых» (Р-07 «Злотых»).

Отвергнуто:
- **всё, что считает приложение, — впрок** — самое чувствительное уходит без потребителя;
- **без денег вовсе** — «требует внимания» и хроника «Злотых» остались бы пустыми.

**Цена.** Каждый новый потребитель денег — правка договора: ступень 3 перечисляет состав поимённо.

---

### Я-14 · Свободный текст человека в срез не идёт; названия — только названному потребителю
24.09.2026

Повод: вычитка «Делу Время» — тексты заметок и пунктов плана в срез не идут, в них чужие имена и ссылки (`planFact.repeats`, `recall`). Но их же обзор хочет «что досмотрел» — это названия, а название эпизода болезни у «Дневников» — по сути диагноз.

**Решено.** Правило одно на все приложения:

- **Свободный текст человека не идёт в срез никогда:** заметки, комментарии, пункты плана, `note` у записей, повторы плана с текстом
- **Названия — фильма, блюда, позиции цикла, болезни — только названному потребителю и в названном составе,** тем же порядком, что суммы в ступени 3 Я-13
- Имена людей — никогда (Я-13)

Выбор человека из трёх, рекомендация.

**Почему.** Свободный текст пишется для себя и несёт чужое — имена, ссылки, подробности; в каком он виде, заранее не сказать. Название — одна строка о вещи, и без него хроника метаприложения безымянна. Но одна строка тоже бывает диагнозом, поэтому названия — по запросу, а не впрок.

Отвергнуто:
- **никаких текстов** — хроника и «что досмотрел» без названий;
- **тексты можно** — срез в том же приватном репозитории, но чужие имена из заметок оказались бы у читателя всех приложений.

**Цена.** Граница «название / свободный текст» проводится поимённо, полем: у каждого приложения — список полей-названий, которые можно отдать.

---

### Я-15 · Потребитель среза показывает, а не досчитывает и не оценивает. Предварительно
24.09.2026

Повод: вычитка «Делу Время». Приложение осознанно отказалось от серий «N недель подряд» (Р-45 «Делу Время»), от «в среднем в день» (Р-55 «Делу Время») и от цвета по виду категории «праздное» (Р-05, Р-57 «Делу Время»). Метаприложение, досчитав их из среза, вернуло бы отвергнутое.

**Решено.**

- **Числа, вердикты и оценки даёт только хозяин данных.** Потребитель показывает их вместе с основанием и не выводит из среза новых: ни серий, ни средних на день, ни цвета, ни вердикта, которого хозяин не дал. Продолжение Я-11
- **Аналитика связей между приложениями** — «ел не то — заболел» — не подпадает и решается отдельно, когда появятся данные (Я-10)
- **Предварительно.** Человек: пересмотреть по ходу работы над метаприложением и на настоящих данных. Записано в открытые вопросы

Выбор человека из трёх, рекомендация.

**Почему.** Иначе правила показа приложения держатся памятью чужого репозитория: запрет на серии записан в «Делу Время», а нарушать его будет метаприложение. Правило договора держит их в одном месте.

Отвергнуто:
- **правило метаприложения, не договора** — запреты хозяина живут в чужих документах;
- **запреты поштучно в срезе** — форма среза растёт списком того, чего нельзя.

**Цена.** Метаприложение беднее: даже безобидное «в среднем» — только если его отдаёт хозяин.

---

### Я-16 · Срез — файл `summary.json` в репозитории данных приложения; пишет проход синхронизации, читает метаприложение токеном только на чтение
24.09.2026

Повод: Этап 3, п. 2 — где лежит срез и как читается (Я-11 оставил открытым). Материал — `07-Разведка`, §2, §8, «Что видно», п. 6.

**Решено.**

- **Срез — файл `summary.json` в корне репозитория данных приложения.** Пишет его проход синхронизации ядра: из тех же данных `readAll`, из которых собираются файлы хранилищ, чистой функцией приложения, переданной конфигом. Функции нет — файла нет: приложения переходят на срез по одному
- **Приложение срез не читает.** Для `storeOf` это не файл хранилища: не скачивается и не вливается. Отправляется, как любой файл прохода, только если разошёлся с лежащим
- **Срез считается только из синхронизируемых записей и дня расчёта.** `settings` устройства — отметки «не было» «Трапезы», идущий таймер «Делу Время», пороги обзора — в расчёт не идут. Это не вкус, а механика: иначе два устройства с одинаковыми данными дадут разный файл и будут переписывать его друг за другом на каждом проходе
- **Свежесть — двумя днями, не временем.** `computedOn` — день расчёта, «посчитано ДД.ММ». `lastEdit` — день самой поздней правки записи по `updatedAt`, «по записям по ДД.ММ»; выводится из данных и одинаков на всех устройствах. Время расчёта с точностью до секунды меняло бы файл на каждом проходе — коммит на каждый проход; день — не чаще раза в сутки
- **Читатель — метаприложение, fine-grained токеном только на чтение** (Contents: read) ровно к репозиториям данных семьи. Токен свой на каждом его устройстве, хранится в его `settings`, не синхронизируется. Имена репозиториев — в настройках метаприложения, а не в коде: у друга свои (`07-Разведка`, §8)

Выбор человека из двух, рекомендация.

**Почему.** Ни новой инфраструктуры, ни второго токена у приложений: срез едет тем же коммитом, что и данные, и в том же приватном репозитории не раскрывает сверх того, что там уже лежит (Р-03 «Злотых», «Цена», п. 3; Я-13). Проход синхронизации уже держит все записи в руках и уже умеет не коммитить неизменное.

Отвергнуто:
- **общий репозиторий срезов** — токен метаприложения видел бы только срезы, и утечка его не раскрыла бы сырых данных. Цена — два коммита за проход, второй репозиторий в токене каждого устройства каждого приложения, новый сбой «данные ушли, срез нет», и каждое приложение знает имя общего места. Если риск в читателе станет настоящим — переезд меняет путь, а не форму;
- **срез в IndexedDB на общем origin** — одно устройство, соединение мешает миграции соседа (Я-11).

**Цена.**

1. Срез свеж настолько, насколько недавно приложение открывали с сетью: сам по себе он не пересчитывается.
2. Устройство со старой сборкой и с новой, пока старое не обновится, переписывают срез друг за другом — лишний коммит на проход.
3. Утечка токена метаприложения раскрывает сырые данные всех репозиториев, к которым он выпущен.
4. README давних репозиториев данных о `summary.json` не скажет: README не переписывается (Р-69 «Делу Время»).

---

### Я-17 · Форма среза общая и описывает себя сама: договор задаёт форму показателя, а не перечень
24.09.2026

Повод: Этап 3, п. 2 — форма среза. Материал — `07-Разведка`, «Что видно», пп. 1–9; Я-12…Я-15.

**Решено.**

- **Договор задаёт форму показателя, а не перечень показателей приложений.** Показатель — машинный `key`, подпись `label`, значение, основание `basis` словами. Метаприложение показывает любую строку и моделей приложений не знает (Я-11, Я-15). Состав по приложениям — таблицей в `02-Архитектура`: это названный состав названного потребителя (Я-13, Я-14), и его правка — правка договора
- **Значение — одно из четырёх:** число с единицей; деньги; вердикт `met` / `failed` / `open`; «не известно» с причиной. Основание — у каждого показателя, без исключений
- **«Не известно» — кодом причины и словами**, не нулём и не `null`. Общие коды: `not-provided` — приложение этого пока не считает; `no-data` — записей нет; `not-enough` — меньше порога. Свои коды приложение заводит само (`no-income`, `covered` «Злотых», `month-precision` «Дневников») — всегда со словами
- **«Не ясно» — вердикт `open`** (Р-24 «Трапезы»); вердикт даёт только хозяин (Я-15)
- **Неокончательный итог — у отрезка:** `through` — по какое число число верно; `null` — итог окончательный. Идущая неделя, месяц с недогруженными выписками (Р-26 «Злотых»)
- **Отрезки — всегда четыре:** прошлая и идущая неделя с понедельника, прошлый и идущий календарный месяц (Я-12), явными `from` и `to`. Отрезок, который приложение пока не считает, присутствует и говорит `not-provided` — не пустым местом. Истории в срезе нет: прошлое пересчитывается задним числом (правка справочников «Трапезы», ретро-ввод «Делу Время», курсы «Злотых»); что видело, метаприложение запоминает само вместе с `computedOn`
- **Деньги — целым в минимальных единицах с кодом ISO 4217.** «Дневники» умножают рубли на сто и пишут `RUB` сами (Р-38 «Дневников»: кода валюты у них нет). Базовая валюта «Злотых» может смениться (Р-41 «Злотых») — код у каждого числа
- **Версия формы — `format: 1`** в самом файле. Правка формы — решение Я-NN и новый номер

Выбор человека из двух, рекомендация.

**Почему.** Типизированные разделы по приложениям сделали бы ядро знатоком всех четырёх моделей — то, от чего Я-11 уводил, — и каждая правка перечня была бы правкой типов ядра и релизом на всех. Показать строку «минуты „Движение“ — 340, учёт в 6 днях из 7» метаприложению не нужно знать, что такое блок времени.

Отвергнуто:
- **типизированные разделы** — ядро знает поле каждого приложения; перечень меняется релизом на всех;
- **история в срезе** — прошлое у трёх приложений из четырёх переписывается задним числом без новых записей (`07-Разведка`, «Вычитка»);
- **деньги десятичной дробью** — «Злотые» хранят целые в минимальных единицах, дробь теряет копейки на сложении.

**Цена.**

1. Метаприложение не проверит, что `key` осмыслен: ошибка приложения в подписи дойдёт до экрана.
2. Основание словами — показать можно, сравнить машинно нельзя. По Я-15 и не нужно.
3. Сопоставление строк по `key` держится на устойчивости ключей: переименование группы «Делу Время» меняет ключ, и настройку человека у метаприложения придётся поправить.

---

### Я-18 · «Требует внимания» в срезе — закончившиеся дни и состояния с датой; «сегодня» остаётся напоминаниям приложений. Уточняет открытый вопрос об общем напоминании
24.09.2026

Повод: открытый вопрос `05` «Общее напоминание через метаприложение»; `07-Разведка`, §4, «Что видно», п. 5; позиция сессии «Трапезы».

**Решено.**

- **В срез идёт то, что верно и после расчёта:** закончившиеся дни и состояния, всегда с днём, к которому относится. «Трапеза» — вчерашние пропуски приёмов по записям, без отметок «не было»; «Делу Время» — обзор прошлой недели не проведён; «Дневники» — число просроченного в циклах, открыт ли эпизод болезни и с какого дня, число зависшего в «смотрю»; «Злотые» — за прошлый месяц нет записей, регулярные ждут
- **Всё про «сегодня» остаётся напоминаниям самих приложений:** «сегодня ничего» «Трапезы», тема `day` «Делу Время». Срез устаревает на первой же синхронизации после расчёта, а они — нет
- **Форма — без текста темы:** `{ key, label, count, day, link }`. Тексты тем несут названия и диагнозы (Я-14); `label` — нейтральная подпись без названий, `link` — путь хеш-роутинга приложения, как у `FeedItem`
- **Выключение тем у приложений сейчас не решается.** Метаприложение показывает «требует внимания» на своём экране; напоминания приложений не трогаются. Как приложение перестаёт напоминать о теме из среза, когда уведомляет метаприложение, — решение вместе с его уведомлением: иначе у человека без метаприложения напоминания пропадут, а у человека с ним станет пять

Выбор человека из двух, рекомендация.

**Почему.** Против общего напоминания в открытом вопросе стояло: срез свеж на момент синхронизации, а темы о времени суток врут. Деление по «закончилось / сегодня» снимает довод без исключений по приложениям: закончившийся день не меняется от того, что срез прочитали через сутки.

**Цена.** Одно уведомление вместо четырёх — не сейчас: пока метаприложение не уведомляет, приложения шлют свои, как раньше.

---

### Я-19 · Позиции сессий приложений к срезу приняты. Промежуточно
24.09.2026

Повод: позиции вычитки (`07-Разведка`, «Вычитка»), которые решает человек.

**Решено** — все пять, как предложены сессиями:

1. **Месяц «Трапезы» — новый расчёт и новое решение «Трапезы»,** а не вынос готовой функции. Пока его нет — отрезок месяца `not-provided`. Неделя — сразу
2. **Тема `day` «Трапезы» в срезе — только вчерашние пропуски, без отметок «не было»,** и это названо в основании. Ошибка одна и в одну сторону — лишнее «вчера не записан обед». Отметки в синхронизируемое хранилище не переносятся (Р-29 «Трапезы» в силе). Следует и из Я-16
3. **Здоровье «Дневников» — ступенями, как деньги в Я-13:** всегда — открыт ли эпизод, число просроченного, число зависшего; названному потребителю — счёты эпизодов и дни болезни в промежутке, без названий, симптомов и заметок; измерения — не в срез. Никогда — название эпизода, `note`, симптомы, `comment` контента. **Метаприложение — названный потребитель второй ступени** ради дней болезни в неделе — первого факта обзора «Делу Время»
4. **`contentStats` по промежутку и «дни болезни в промежутке» — новые решения «Дневников».** Контент — на `Period` ядра, «досмотрено» — нынешний статус начатого в промежутке, плюс «ещё N с точностью до месяца»; неделя — «не известно», `month-precision`. Дни болезни — пересечение эпизодов с промежутком, наложения склеены, открытый длится до дня расчёта
5. **Сопоставление с соседями — по группе, настройкой человека у метаприложения.** Метаприложение ставит строки рядом и **не складывает между приложениями** (Я-15): стрижка — отметка «Дневников» и расход «Злотых», сложенная — посчитана дважды

**Промежуточно** (человек, 24.09.2026): принято, чтобы начать; уточняется по ходу реализации в приложениях и метаприложении. Записано в открытые вопросы.

**Почему.** Каждая позиция — хозяина данных о его данных (Я-11); сессии знают свои решения лучше разведки.

**Цена.** Три новых расчёта в двух приложениях до того, как срез полон: месяц «Трапезы», контент по промежутку и дни болезни «Дневников».

---

### Я-20 · У «требует внимания» — основание, как у показателя. Уточняет Я-18
24.09.2026

Повод: сборка промптов приложениям после кода среза (`d049c2c`). Я-19, п. 2 требует, чтобы тема `day` «Трапезы» в срезе называла в основании, что отметки «не было» не учтены. Но форма `Attention` из Я-18 — `{ key, label, count, day, link }` — основания не имеет, хотя `count` — число, а правило «число — вместе с основанием» (Я-17) общее.

**Решено.**

- У `Attention` — обязательное `basis`: основание словами, как у `Metric`. «Вчера по записям нет обеда; отметки „не было“ не учтены», «по статусам позиций на день расчёта»
- `format` остаётся `1`: срез ещё не пишет ни одно приложение и не читает ни один потребитель, настоящих файлов старой формы нет

Выбор человека, рекомендация.

**Почему.** Иначе у одного вида чисел в срезе основания нет, и оговорку «Трапезы» некуда положить, кроме подписи, — где метаприложение её не отличит от названия темы.

Отвергнуто: **оговорка в `label`** — подпись и основание смешаны, правило Я-17 не везде.

**Цена.** Правка формы через час после её записи в код: коммит ядра сверх `d049c2c`, указатель у приложений — сразу на новый.

---

### Я-21 · Как читать срез: закончившийся отрезок не заморожен; нет пункта «требует внимания» — не «всё сделано». Уточняет Я-17, Я-18
24.09.2026

Повод: первый настоящий срез — «Делу Время», Р-87 «Делу Время», код `c0ffcc2`, ядро `b0047b6`. Их сессия проверила `summary.json` из репозитория данных `parseSummary` ядра и принесла «не сошлось с договором».

**Как было.** Я-17: `through: null` — «итог окончательный». Но ретро-ввод правит прошлую неделю (Р-19 «Делу Время»), и то же у всех: справочники «Трапезы», курсы «Злотых», переименования «Дневников» (`07-Разведка`, «Вычитка»). Ещё: `review` «Делу Время» отдаёт только в тот день, когда зовёт к обзору само приложение, — в понедельник; во вторник срез говорит пустой список, хотя обзор так и не проведён.

**Решено.**

- **`through: null` — отрезок закончился, а не «число заморожено».** Число закончившегося отрезка — лучшее, что хозяин знает на день расчёта; задним числом оно пересчитывается у всех приложений. Свежесть — `computedOn` и `lastEdit`, не `through`. Форма и `format` те же; меняются слова в `02` и комментарий в `core/summary.ts`
- **Пункт «требует внимания» есть — хозяин зовёт; пункта нет — не зовёт.** Это не «всё сделано»: когда звать, решает хозяин данных по своим правилам (Я-11, Я-15). Метаприложение отсутствие пункта не толкует
- **Состав в договоре — семействами ключей, со ссылкой на таблицу приложения.** Полная таблица ключей, подписей и оснований — в `02-Архитектура` приложения; ядро не держит её копией. Ключи «Делу Время» укладываются в первый состав Я-19 — это уточнение, а не правка состава
- **Не добавляются:** нормы месяца «в N из M недель» «Делу Время» — их не просит ни один потребитель (Я-13: не впрок), придут правкой состава по просьбе метаприложения; строка о `summary.json` в README давних репозиториев — цена Я-16, п. 4, README человека (Р-69 «Делу Время»), дописать можно руками

Выбор человека, рекомендация.

**Почему.** Поле «больше не изменится» честно не заполнит ни одно приложение: ретро-ввод и правка справочников возможны всегда. Правило «пункта нет — значит, в порядке» подменило бы правило хозяина правилом читателя — то, от чего уводит Я-15.

**Цена.** Метаприложение не может сказать «за прошлую неделю — окончательно»; только «посчитано ДД.ММ, по записям по ДД.ММ».

---

### Я-22 · «Дача» — в семью и сразу на ядре; тотализатор для друзей — вне семьи
24.09.2026

Повод: человек планирует два новых проекта — «Дачу» (схема участка для планирования посадок, дела с приоритетами; пока сыро) и тотализатор для друзей (бытовые ставки внутри компании: «Олег не курит до 31 января 2027»). Открытый вопрос `05` «Берёт ли „Дача“ ядро» (План «Трапезы»).

**Решено.**

- **«Дача» — приложение семьи, начинается сразу на ядре**, как «Злотые»: копию ядра не заводит. Свой `dbName` и свой приватный репозиторий данных; схема участка и дела — модули приложения. Её `dbName` вписывается в договор («Устройство и origin») сессией ядра при старте «Дачи». Срез итогов — по Я-16…Я-21, когда будет что отдать; «требует внимания» — естественный первый кандидат
- **Тотализатор — не приложение семьи и ядра не берёт.** Договор семьи — один человек, свой приватный репозиторий, свой токен на устройстве, слияние «побеждает поздняя правка». У ставок с друзьями данные общие на нескольких людей, и важно, кто что поставил и кто закрыл спор: «поздняя правка побеждает» дала бы любому перезаписать чужую ставку. Ему нужен общий сервер или бот в общем чате — это решает его собственный проект. Метаприложение его не читает
- **Без денег, на баллы** — совет человеку, не правило ядра: денежные ставки попадают под закон об азартных играх

Выбор человека, рекомендация.

**Почему.** Граница семьи — форма данных, а не тема: «Дача» — учёт одного хозяйства с синхронизацией между своими устройствами, ровно то, что ядро делает; тотализатор — общий учёт нескольких людей, которого ядро не делает и делать не должно — правило границы (`CLAUDE.md`): ради одного приложения ядро не правится.

Отвергнуто:
- **«Дача» отдельно, без ядра** — пятая копия механики, от которой ядро и родилось (Р-45 «Трапезы»);
- **тотализатор в семье** — общий репозиторий с доступом на запись у всех друзей, токены друзей к приватному репозиторию человека, слияние, которое молча теряет чужие ставки.

**Цена.** «Дача» — пятое приложение на origin и пятый потенциальный срез: метаприложение не должно знать список приложений кодом (Я-16 — имена репозиториев в настройках).

---

## Открытые вопросы

- **Граница каркаса стилей и место разреза `notify`** — первая нарезка при выносе, проверка — при переводе «Трапезы» (Р-48 «Трапезы», «Цена» п. 2 и 4).
- **Тесты ядра внутри приложения** — включать их в `vitest` и `tsc` приложения или исключать (Я-02, «Цена» п. 1). Решается при переводе «Трапезы».
- **«Итоги» в договоре — форма есть, состава и кода нет.** Где лежит и как читается — Я-16; форма — Я-17; «требует внимания» — Я-18, Я-20; как читать — Я-21; код ядра — `d049c2c`. Первый настоящий срез — «Делу Время» (Р-87 «Делу Время»). Остаётся: функции среза в «Трапезе», «Дневниках», «Злотых», первый настоящий читатель — метаприложение (План, Этап 3).
- **Я-15 — пересмотреть на деле.** «Потребитель показывает, а не досчитывает» принято предварительно: вернуться по ходу работы над метаприложением и на настоящих данных (человек, 24.09.2026).
- **Общее напоминание через метаприложение** (мысль человека, 24.09.2026). Что берётся из среза, а что остаётся приложениям, — Я-18. Открыто: как приложение перестаёт напоминать о теме из среза, когда уведомляет метаприложение, и не теряет напоминаний у человека без него. Решается вместе с уведомлением метаприложения.
- **Я-19 — промежуточно.** Позиции сессий приложений приняты, чтобы начать; уточняются по ходу реализации срезов в приложениях и метаприложении (человек, 24.09.2026).
- **README репозиториев данных** — собирается ядром из `name` и `storeNotes`, а текст у трёх приложений разный; сведение не обсуждалось (Журнал «Трапезы», 18.09.2026).
