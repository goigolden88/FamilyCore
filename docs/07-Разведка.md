# Разведка: что приложения семьи могут отдать срезу итогов

Этап 3, п. 1 (Я-10, Я-11). Материал к решению «где лежит срез, как читается, какой он формы». Не решение и не договор: здесь — что есть в коде приложений сегодня.

**Как собрано.** 24.09.2026, отсюда, через `git show HEAD` — только чтение:

| Приложение | Путь на диске | Коммит |
|---|---|---|
| «Трапеза» | `Еда/Trapeza` | `4a9a8bb` |
| «Делу Время» | `ТаймМенеджмент/DeluVremya` | `ed95582` |
| «Дневники» | `Дневники/Dnevniki` | `fba632a` |
| «Злотые» | `Злотые` | `fd9c68f` |

Анкета — восемь пунктов записки сессии «Дневников» о метаприложении. Их записка сверена с кодом, расхождения — в разделе «Дневники». Сводку каждого приложения ещё вычитывает его сессия: из кода не видно решений, которые в нём не записаны.

Репозиторий ядра публичный: здесь только устройство кода, ни одной записи из настоящих данных.

---

## 1. Данные и особенности дат

| | Записи с датой | Справочники | Схема | Особенности дат |
|---|---|---|---|---|
| «Трапеза» | `intake` — блюдо в приёме дня: `date`, `meal`, порции или граммы | категории, блюда, шаблоны, нормы недели | 1 | дата обязательна, день еды, а не ввода; `at` ЧЧ:ММ — по желанию |
| «Делу Время» | `time` — блок минут на категорию; `notes` — дело, мысль, замысел, пункт плана; `reviews` — проведённый обзор недели | категории с видом `useful`/`neutral`/`idle` и нормой недели, пресеты, шаблоны дня | 1 | `capturedOn` и `plannedFor` бывают `null` — «дата неизвестна» и «во входящих» (Р-08 «Делу Время»); `doneOn` — день факта; фоновая категория у блока в сумму не входит (Р-43 «Делу Время») |
| «Дневники» | `cycleEvents`, `episodes`, `measures`, `sessions`, `content` | позиции циклов, категории, теги, быстрые кнопки | 2 | месячная дата `ГГГГ-ММ` у контента — не первое число (Р-25); `start = null` у контента — намерение, а не событие; эпизод — отрезок, открытый длится до сегодня; кривая дата не выбрасывается (Р-34, Р-84) — всё «Дневников» |
| «Злотые» | `entries` — операция или **итог периода** `period: { from, to }` вместо даты; `balances` — снимок остатка; `rates`; долги — события, траты, переводы, займы, возвраты | валюты, счета, категории, регулярные, профиль, люди, комнаты | 3 | итог периода по месяцам не дробится (Р-12 «Злотых», п. 6); перевод между своими — не расход и не доход; суммы — целые в минимальных единицах, **валюты не складываются** (Р-04 «Злотых») |

## 2. Где лежит

Все на origin `goigolden88.github.io`, у каждого свой `dbName` и приватный репозиторий данных. Раскладка — договор семьи (`02-Архитектура`):

| | `dbName` | Одним файлом | По месяцам | По годам |
|---|---|---|---|---|
| «Трапеза» | `trapeza` | справочники | `intake/` | — |
| «Делу Время» | `deluvremya` | справочники, `reviews.json` | `notes/` (по `capturedOn`), `time/` | — |
| «Дневники» | `dnevniki` | справочники, `health/*.json` | — | `cycles/`, `content/` (по `start`) |
| «Злотые» | `zloty` | справочники, `profile.json` | `rates/`, `entries/`, `balances/`, пять папок долгов, `notes/` | — |

Читатель раскладки обязан знать все три нарезки (Я-09, «Цена», п. 3). По п. 2 Я-11 сырые файлы метаприложению не нужны. Раскладка важна, только если срез ляжет файлом в тот же репозиторий.

## 3. Готовые итоги — чистые функции

У всех четырёх итоги уже посчитаны чистыми функциями без React и без `db`, покрыты тестами. Различаются **зерно** — за какой отрезок — и форма.

| | Функция | Зерно | Что отдаёт |
|---|---|---|---|
| «Трапеза» | `summarize(records, dishes, categories)` | любые записи | порции по группам и категориям; `assumed` — посчитано одной порцией без веса (Р-18 «Трапезы»); `kcal: KcalSum` |
| | `weekSummary(intake, dishes, categories, day, today)` | неделя | то же за неделю + дни, приёмы, `logged` из `elapsed` — «учёт в 6 днях из 7» |
| | `sumKcal` → `KcalSum { kcal, counted, total }` | любые записи | ккал вместе с основанием |
| | `checkWeek`, `normHistory` | неделя | `Verdict`: `met` / `failed` / **`open`** — исход зависит от дней без записей (Р-24 «Трапезы») |
| «Делу Время» | `periodSummary(blocks, categories, period, today)` | **любой промежуток** (Р-52 «Делу Время») | минуты, блоки, дни, по категориям и по виду; фон отдельно |
| | `planFact(notes, period, today)` | любой промежуток | план против факта: запланировано, сделано, с опозданием, главное дело, оценки, повторы |
| | `weekNorms`, `periodNorms`, `checkNorm` | неделя | нормы с тем же «неясно», образец для «Трапезы» |
| | `daySummary` | день | итог дня по категориям |
| «Дневники» | `cycleStates(items, events, day)` | **на день** | статус позиции `overdue` / `due` / `ok` / `never` / `unset`, давность, интервал, следующий раз |
| | `spent`, `spendTree` → `Spent` | любые отметки | траты вместе с числом отметок (Р-38) |
| | `healthStats(episodes, period, now)` | промежуток — **свой `Period`** с необязательными концами | число болезней, закрытых, средняя и медианная длительность, промежутки, дни без болезни |
| | `openEpisodes`, `activityTotals(sessions, period)`, `series(measures, metric)` | на день / промежуток / весь ряд | |
| | `contentStats(entries, year)` | **год** или вся история | начато, досмотрено, брошено; закончено по статусу (Р-42); средняя оценка с числом оценённых |
| | `staleWatching(entries, today)` | на день | зависшее в «смотрю» |
| «Злотые» | `monthReport(data, month, today, through)` | **месяц** | доход, расход, отложено — `Sum { amount, entries, missing }` в базовой валюте |
| | `savingsRate(report)` | месяц | доля, «в N раз» или «нет» — доход не внесён ≠ ноль |
| | `usualMonth`, `overUsual`, `growthByCategory` | прошлые месяцы | «обычный месяц» только с достаточного числа месяцев (Р-21 «Злотых») |
| | `capitalOn` → `Capital` | **на дату** | сбережения, вложения, отложенные платежи; у каждого счёта — дата снимка, по которому взят (Р-39 «Злотых») |
| | `debtTotals`, `debtsByPerson` | на сейчас | долги по людям — **списком сумм по валютам**, не одним числом |

## 4. «Требует внимания» — темы напоминаний

Форма у всех одна — `Topic` ядра: `{ notice: { title, body } | null, tag, target, loudKey, quietKey }`. Тем восемь:

| | Тема (`tag`) | Когда говорит | Читает сверх записей |
|---|---|---|---|
| «Трапеза» | `day` | вчера не записаны основные приёмы, сегодня пусто | **«не было» — отметки этого устройства** (`SKIPPED_KEY`, Р-29 «Трапезы») |
| «Делу Время» | `day` | за сегодня время не учтено | **имя экрана этого устройства** (Р-26 «Делу Время») |
| | `review` | воскресенье и понедельник, обзор недели не проведён | — |
| «Дневники» | `overdue` | просрочено в циклах | — |
| | `illness` | открыт эпизод болезни | — |
| | `content` | зависло в «смотрю» — **не чаще раза в неделю** | **день прошлого вопроса — настройки устройства** |
| «Злотые» | `month` | с N-го числа прошлый месяц не внесён | — |
| | `recurring` | регулярные этого месяца ждут | — |

Фоновую проверку браузер будит сам, примерно раз в сутки, время выбрать нельзя (`notify.ts` ядра). Что он будит приложения одного origin разом — вывод из наблюдения человека (четыре уведомления в одно время), а не из кода. Окно со звуком — `DEFAULT_WINDOW` 12…20, у каждого приложения своё.

**Для общего напоминания через метаприложение это главное:** три темы из восьми зависят от настроек устройства, которые не синхронизируются. Срез, посчитанный на одном устройстве, у другого скажет иное.

## 5. Лента

`registry.feedItems` есть у всех четырёх и отдаёт `FeedItem` договора. У «Злотых» — `feedItems(data)` без дня, у остальных — `(data, day)`. Для хроники годится; считать по ней нельзя (`02-Архитектура`, «Чего в договоре нет»).

## 6. Правила показа чисел — к ним пришли все четыре независимо

| Правило | «Трапеза» | «Делу Время» | «Дневники» | «Злотые» |
|---|---|---|---|---|
| **Число — вместе с основанием** | Р-01: `KcalSum`, «учёт в 6 днях из 7» | `MARKS_BASIS`, `PLAN_FACT_BASIS`, `YEAR_NORMS_BASIS` | Р-38, Р-44: сумма с числом отметок, среднее с числом оценённых | Р-07: `Sum.entries`, «по 142 операциям» |
| **Неизвестное — не ноль** | ккал без данных — вне суммы и названы | — | — | «доход не внесён» вместо нуля; без курса — вне итога и названо (`missing`) |
| **Третий исход «не ясно»** | `Verdict 'open'` — дни без записей | нормы с неизвестными днями | — | — |
| **Догадка не выдаётся за расчёт** | нормы — с `NORM_MIN_WEEKS` | `NORM_MIN_WEEKS` | срок по истории — с трёх промежутков (Р-27) | «обычный месяц» — с `USUAL_MONTHS`, блок молчит и говорит, чего ждёт (Р-21) |
| **Не прятать молча** | итог называет, сколько «без категории» (Р-02) | в сравнении категория прошлого — с нулём (Р-55) | фильтр показывает счёт свёрнутого (Р-46, Р-55) | не вошедшее названо (Р-04) |

## 7. Кто ещё ждёт среза

- **Недельный обзор «Делу Время»** (Р-50 «Делу Время»; Р-79 «Дневников»). В их Плане, «Отложено», — набросок: болезни недели против недобора норм; тренировки «Дневников» против категорий движения — одно занятие в двух местах; досмотренное против времени на «Фильмы», «Игры»; обслуживание за неделю; траты на досуг против времени досуга. Путь интеграции — «после всего семейства, метаприложением» (человек, 14.09.2026). По их Р-50 путь выбирается, когда обзор проживёт четыре недели
- **Связи, отложенные «Трапезой»** (Р-05 «Трапезы»): «ел не то — заболел», «калории против веса», «еда против тренировок». Для них нужны месяцы данных, они вне Я-10
- **Общая хроника с поиском** — все приложения, метаприложение (План «Делу Время»)

## 8. Риски и ограничения

- **Чувствительность.** Здоровье — «Дневники»; деньги, остатки на счетах — «Злотые»; заметки и планы — «Делу Время». Метаприложение, читающее всех, собирает всё это в одном месте
- **Приложения отдаются другу** (Р-67 «Делу Время»; отдача «Злотых» — вместе с семьёй). У друга свои репозитории данных: метаприложение не должно знать их имена кодом
- **Путь чтения** — открыт после Я-11. Файл среза в репозитории данных означает токен на чтение четырёх приватных репозиториев и сеть. Прямо из IndexedDB — в обход договора, только на одном устройстве, и соединение мешает миграции соседа
- **Два `Period`.** У ядра — `{ from, to }` (`core/dates.ts`), у здоровья «Дневников» — свой, с необязательными концами. Контент «Дневников» — по году, «Злотые» — по месяцу, «Трапеза» — по неделе

---

## Расхождения с запиской «Дневников»

- `contentStats(entries, year)` — **по году** или по всей истории, а не за промежуток
- `healthStats` и `activityTotals` берут **свой** `Period` из `modules/health/health.ts` с необязательными концами, а не `Period` ядра
- Остальное сошлось: пять видов событий, шаг оценки 0.1 (Р-26), `cycleStates(items, events, day)` со статусами `overdue`…`unset`, `spent` и `spendTree`, `openEpisodes`, `series`, `staleWatching`, `feedItems(data, day)`, три темы напоминаний

## Что видно для формы среза — наблюдения, не решение

1. **«Число с основанием» — общий знаменатель всех четырёх.** Каждый показатель среза, вероятно, несёт основание, а не голое число. Это первый кандидат в договор
2. **Показатель бывает «не ясно» и «не известно»** — не ноль. Форма среза должна уметь сказать это без выдумывания числа
3. **Зерно разное:** день, неделя, любой промежуток, месяц, год, «на дату». Общего отрезка, на котором считают все, нет
4. **Деньги — списком по валютам,** один показатель может быть несколькими числами
5. **«Требует внимания» уже одинаково по форме** (`Topic`, `Notice`) — самая готовая часть. Но три темы из восьми зависят от настроек устройства
6. **Свежесть.** Срез, записанный приложением при синхронизации, свеж на её момент. Для «на дату» — циклы, болезнь, капитал — это видно сразу

## Вопросы на вычитку сессиям приложений

- **Всем:** верно ли прочитаны функции итогов; какие из них вы сами назвали бы итогом для чужого глаза, а какие — внутренней кухней экрана
- **«Трапеза»:** можно ли считать тему `day` без отметок «не было» этого устройства — или без них она врёт
- **«Делу Время»:** какие факты соседей обзор недели хочет увидеть первыми — таблица «Отложено» всё ещё верна?
- **«Дневники»:** два `Period` — нарочно или наследство; можно ли `contentStats` по промежутку
- **«Злотые»:** можно ли вообще отдавать деньги в срез, который читает кто-то кроме приложения, и в каком виде — суммы, доли или только «внесён ли месяц»
